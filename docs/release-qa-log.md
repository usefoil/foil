# Release QA Log

Use this file as the release-candidate evidence template. Copy the checklist
below into the release PR or release notes, then fill every result before
publishing.

## Current Public Install Status

- Current public beta: Foil `v1.13.10` build `45`.
- Primary install path: Homebrew tap `mean-weasel/foil`, backed by public tap repository `mean-weasel/homebrew-foil`.
- Verified command:
  `brew tap mean-weasel/foil https://github.com/mean-weasel/homebrew-foil`
  then `brew install --cask foil`.
- Manual fallback: GitHub release asset `Foil-1.13.10-macos.dmg`, verified against `Foil-1.13.10-macos.dmg.sha256` and release asset digest.
- Public cask status: `Casks/foil.rb` version `1.13.10`, SHA-256 `1d025a1fb0cecabd472df6e986fc0b6555635f98de1f2a850030597a21a5d0f6`, matching the GitHub release DMG digest.
- Release/cask metadata verified on 2026-07-05 with `gh release view --repo usefoil/foil` and `gh api repos/mean-weasel/homebrew-foil/contents/Casks/foil.rb`.
- Latest recorded public cask extraction smoke in this log remains the v1.13.9 `REQUIRED_COMMIT=0dcc3287327ae91767bcfe3f806de4ce8fc852d5 make check-production-permissions-cask` run; v1.13.10 installed-app identity evidence is recorded under "v1.13.10 TCC Matrix Continuation."
- Remaining external smoke: run a true fresh-machine or disposable fresh-user onboarding walkthrough; tracked in issue #154 with the runbook in `docs/fresh-machine-homebrew-onboarding-smoke.md`.

## Test Command Policy

- `make test` and CI unit tests are deterministic by default and skip
  `FoilTests/LiveGroqIntegrationTests`, even if a local shell or runner
  environment contains stale `RUN_LIVE_GROQ_TESTS` or `GROQ_API_KEY` values.
- Record live Groq API XCTest evidence separately with
  `RUN_LIVE_GROQ_TESTS=1 GROQ_API_KEY=... make test-live-groq`. Do not paste the
  key into this log, PRs, issues, or CI summaries.
- App-level live Groq provider QA remains `make test-provider-qa-live`; live
  local transcription remains `make test-local-transcription-e2e`.

## Pre-Release Feature Hardening: Cleanup Groups, Usage Insights, Recent Apps

Date: 2026-07-05

Scope: unreleased production-code hardening after PR #357 and PR #358 merged into `origin/main` at `bfa0b34`. This section does not claim a packaged public release exists.

| Claim | Strongest realistic failure mode | Evidence | Result |
| --- | --- | --- | --- |
| Transcription-derived usage metadata can drive later cleanup group routing. | Seeded UI tests pass, but a real controller usage event cannot become a recent-app assignment that affects the next transcription. | Added `FoilTests/TranscriptionControllerTests.testUsageTopAppCanDriveCleanupGroupAssignmentAndLaterRouting`; focused run passed 1 test in `Test-Foil-2026.07.05_14-15-51--0700.xcresult`. The broader focused controller run passed 4 tests in `Test-Foil-2026.07.05_14-16-01--0700.xcresult`. | PASS |
| Usage Insights and Cleanup Settings render the seeded user-facing states. | Controller/model tests pass, but Insights or Recently used apps UI is missing or inaccessible. | `xcodebuild test -project Foil.xcodeproj -scheme Foil -only-testing:FoilUITests/FoilUITests/testCleanupGroupAppPickerShowsRecentlyUsedAppsFromUsageEvents -only-testing:FoilUITests/FoilUITests/testUsageInsightsShowsPopulatedMetricsFromUsageEventStore` passed 2 UI tests in `Test-Foil-2026.07.05_14-16-07--0700.xcresult`. | PASS |
| Usage metrics remain metadata-only and independent from transcript history. | Usage events retain transcript/prompt/vocabulary/API key/audio path/app path/base URL/error content, depend on transcript history, or delete transcript history when cleared. | Source scans of `UsageEventStore`, usage write callers, UI seeds, Insights, Settings, and tests; `xcodebuild test -project Foil.xcodeproj -scheme Foil -only-testing:FoilTests/UsageEventStoreTests/testEncodedUsageEventsAreMetadataOnly -only-testing:FoilTests/UsageEventStoreTests/testPersistsUsageEventsWhenTranscriptHistoryPersistenceIsDisabled -only-testing:FoilTests/UsageEventStoreTests/testDeleteUsageEventsDoesNotTouchTranscriptHistoryFiles` passed 3 tests in `Test-Foil-2026.07.05_14-19-18--0700.xcresult`. | PASS |
| Insights stays factual-only for v1 and does not introduce cleanup recommendations before release. | UX polish expands scope into unproved recommendations or cleanup advice. | Direct inspection of `docs/superpowers/specs/2026-07-05-usage-insights-cleanup-groups-design.md`, `Foil/UsageInsightsView.swift`, `Foil/SettingsView.swift`, and `FoilUITests/FoilUITests.swift`; UI test asserts populated Insights does not contain `recommend`. | PASS |

Residual risk: this tranche has deterministic fixture/controller/UI proof, not a live microphone/provider smoke against a packaged Developer ID build. Run final broad local gates and package/release verification before publishing the next release.

## Release Readiness Verification: Local Baseline For `e02b815`

Date: 2026-07-06

Scope: local, non-destructive baseline verification for `main` at `e02b81564f08bc104c9fae5737eadbb45d629200`. This section does not claim a packaged public release, public cask, production install, setup/TCC matrix, or packaged feature smoke exists for this commit.

| Claim | Strongest realistic failure mode | Evidence | Result |
| --- | --- | --- | --- |
| Local checkout is the intended release-readiness source. | Baseline commands run against a stale branch or dirty worktree instead of current `origin/main`. | `git status --short --branch` reported `## main...origin/main`; `git rev-parse HEAD` and `git rev-parse origin/main` both returned `e02b81564f08bc104c9fae5737eadbb45d629200`; `git diff --check` passed. | PASS |
| Notarized QA build helper still passes its static/default validation. | Release QA artifact path could fail before signing/notarization because the helper accepts bad defaults or emits wrong metadata. | `scripts/test-build-notarized-qa-dmg.sh` passed with `PASS: build-notarized-qa-dmg defaults`. | PASS |
| Warning-clean local Debug build still succeeds after merged feature work. | Merge CI passed but current local checkout has warning or build drift that would block release prep. | `make build-warnings-as-errors` passed and printed `** BUILD SUCCEEDED **` on 2026-07-06 09:55 MST. | PASS |
| Deterministic unit suite still succeeds with live Groq disabled by default. | Feature-specific tests passed, but broader deterministic unit coverage regressed or accidentally depended on live credentials. | `make test` passed and printed `** TEST SUCCEEDED **`; xcresult: `/Users/jeremywatt/Library/Developer/Xcode/DerivedData/Foil-ebuxbvgmspyykabsrfclwquwxbdi/Logs/Test/Test-Foil-2026.07.06_09-55-13--0700.xcresult`. | PASS |
| Recent `main` service-side checks are green for the same commit. | Local tests pass but scheduled/live E2E or post-merge checks on `main` are failing or stale. | `gh run list --branch main --limit 5 --json ...` showed success on `e02b81564f08bc104c9fae5737eadbb45d629200` for E2E OpenAI API Tests, E2E Groq API Tests, CodeQL, and Local macOS E2E. | PASS |

Residual risk: full UI diagnostics were intentionally not run in this baseline slice because `make test-ui-diagnostics` drives the active macOS desktop session. No Notarized QA Build, public release artifact, public cask, `/Applications` production install, setup/TCC matrix, or packaged feature smoke has been proven for `e02b815` yet.

## Release Readiness Verification: Artifact Gate For `e02b815`

Date: 2026-07-06

Scope: read-only artifact availability check for `e02b81564f08bc104c9fae5737eadbb45d629200`.

| Claim | Strongest realistic failure mode | Evidence | Result |
| --- | --- | --- | --- |
| Latest public release artifact contains `e02b815`. | Public release/cask assets are still for an older tag, so DMG/signing/appcast checks would prove the wrong build. | `gh release view --repo usefoil/foil --json tagName,...` reported latest release `v1.13.10`; `git rev-list -n 1 v1.13.10` returned `915bf6fe0c82ec95a323c159fa0f5b258f165a09`; `git merge-base --is-ancestor e02b81564f08bc104c9fae5737eadbb45d629200 v1.13.10` exited `1`. | BLOCKED |
| A notarized QA artifact exists for `e02b815`. | A prior QA artifact exists but was built from an unrelated older commit. | `gh run list --workflow 'Notarized QA Build' --limit 20 --json ...` found no run with `headSha` `e02b81564f08bc104c9fae5737eadbb45d629200`; latest listed QA run was `2026-06-25` on `6ca2bece90c5b69b835682ceb68a952014be9b6b`. | BLOCKED |

Required follow-up: trigger **Notarized QA Build** for `ref=e02b81564f08bc104c9fae5737eadbb45d629200` or perform an approved release-prep/tag/Release workflow path. Do not use `v1.13.10` artifacts as proof for the merged cleanup groups / usage insights / recent-app stack.

Update: the QA-artifact follow-up is satisfied by Notarized QA Build run `28811180245`, recorded below. Public release and cask artifacts still remain blocked on a later release/cask update.

## Release Readiness Verification: Notarized QA Artifact For `e02b815`

Date: 2026-07-06

Scope: approved Notarized QA Build artifact verification for `e02b81564f08bc104c9fae5737eadbb45d629200`. This is a QA artifact, not a public release or cask update.

| Claim | Strongest realistic failure mode | Evidence | Result |
| --- | --- | --- | --- |
| Notarized QA Build produced an artifact for `e02b815`. | A workflow run exists, but it targeted the wrong commit or failed before upload. | `gh workflow run 'Notarized QA Build' -f ref=e02b81564f08bc104c9fae5737eadbb45d629200` dispatched run `28811180245`; `gh run view 28811180245 --json ...` reported `conclusion:"success"`, `headSha:"e02b81564f08bc104c9fae5737eadbb45d629200"`, and job `Build Notarized QA DMG` completed successfully. | PASS |
| Downloaded QA DMG matches uploaded checksum. | Artifact download is corrupt or checksum file belongs to a different path. | `gh run download 28811180245 --dir /tmp/foil-notarized-qa-e02b815`; checksum file and local `shasum -a 256` both reported `680eb9e7f0b0fc5025babedb36f7a7326091b2169fbe19ea261067f44071f164` for `Foil-1.13.10-28811180245-e02b81564f08bc104c9fae5737eadbb45d629200-16-macos.dmg`. | PASS |
| QA DMG is notarized and stapled. | Artifact builds but is not accepted by Gatekeeper or lacks a valid stapled ticket. | `spctl -a -vv -t open --context context:primary-signature <dmg>` reported `accepted`, `source=Notarized Developer ID`, origin `Developer ID Application: Mean Weasel LLC (B3A6AN2HA4)`; `xcrun stapler validate <dmg>` reported `The validate action worked!`. | PASS |
| Mounted app has the expected signed identity. | DMG is trusted, but contained app has wrong bundle id, missing Sparkle key, wrong version/build, or bad codesign. | Read-only mount showed `Foil.app`, `.background/dmg-background.png`, and `Applications`; `Info.plist` reported bundle id `com.neonwatty.Foil`, version `1.13.10`, build `28811180245`, and `SUPublicEDKey` decoded to 32 bytes. `spctl -a -vv -t execute` accepted the mounted app as `Notarized Developer ID`; `codesign --verify --deep --strict --verbose=2` passed; `codesign -dv --verbose=4` reported Developer ID Application authority and team `B3A6AN2HA4`. | PASS |

Residual risk: this QA artifact is not a public release, public cask, `/Applications` install, setup/TCC proof, or packaged feature smoke. Continue release-readiness gates against this exact artifact before making release claims.

## Release Readiness Verification: Merge Queue And Mainline QA Artifact For `550506a`

Date: 2026-07-06

Scope: post-repair merge queue and mainline notarized QA artifact proof after PR #360 landed. This is still not a public release or cask update.

| Claim | Strongest realistic failure mode | Evidence | Result |
| --- | --- | --- | --- |
| Route-resolution repair landed on `main` with required merge-queue checks green. | Branch CI and packaged smoke passed, but the merge queue either did not merge the repair or left required mainline checks pending/failing. | PR #360 merged at `2026-07-06T21:27:00Z` with merge commit `550506a0f7c2f38da21b1eaa8cfeab9d2fa5b26f`. Merge-group CI run `28823900947` passed Detect Changed Paths, Build, Unit Tests, Audio UX Snapshots, Focused UI Smoke, and CI Gate. Merge-group E2E runs passed for Groq (`28823901017`), Local macOS E2E (`28823900927`), and OpenAI after rerun (`28823900965`, attempt 2); attempt 1 failed with provider 404 `Invalid URL (POST /v1/audio/transcriptions)`. Post-merge `main` runs then passed CodeQL (`28824420693`) and Local macOS E2E (`28824421220`) on `550506a`. | PASS |
| A notarized QA artifact exists for the actual post-merge `main` commit. | The available QA artifact could be the pre-merge branch build, leaving the merge commit unproved as an installable artifact. | Notarized QA Build run `28824524390` was dispatched for `550506a0f7c2f38da21b1eaa8cfeab9d2fa5b26f`, completed successfully, and uploaded `Foil-1.13.10-28824524390-550506a0f7c2f38da21b1eaa8cfeab9d2fa5b26f-18-macos.dmg`. | PASS |
| Downloaded mainline QA DMG matches checksum and is notarized/stapled. | Artifact download could be corrupt, unsigned, unstapled, or mismatched with the checksum file. | Downloaded to `/tmp/foil-notarized-qa-550506a`; uploaded checksum and local `shasum -a 256` both reported `83a5d0040a04082d99a3af685692f3e76ff15abd251707696f20346b7b028065`. `spctl -a -vv -t open --context context:primary-signature` accepted the DMG as `Notarized Developer ID`; `xcrun stapler validate` reported success. | PASS |
| Mounted mainline QA app has the expected signed identity. | DMG trust could pass while the contained app has the wrong bundle id, version/build, Sparkle key, or signing identity. | Read-only mount `/Volumes/Foil QA` contained `.background/dmg-background.png`, `.DS_Store`, `Applications`, and `Foil.app`. The app reported bundle id `com.neonwatty.Foil`, version `1.13.10`, build `28824524390`, and a 32-byte `SUPublicEDKey`; `spctl -a -vv -t execute` accepted it as `Notarized Developer ID`; `codesign --verify --deep --strict --verbose=2` passed; `codesign -dv --verbose=4` reported Developer ID Application authority, team `B3A6AN2HA4`, and hardened runtime. | PASS |

Residual risk: `/Applications/Foil.app` is still the earlier QA branch artifact unless replaced again. Public release, appcast, Homebrew cask update, public cask extraction/install, `/Applications` public cask install, and fresh-user/TCC matrix are not proven for `550506a` yet.

## Release Readiness Verification: Release Prep For `v1.13.11`

Date: 2026-07-06

Scope: release-prep branch `codex/prepare-v1.13.11-release` for Foil `1.13.11` build `46`. This section does not claim the release tag, public Release workflow, appcast, or public cask has been published.

| Claim | Strongest realistic failure mode | Evidence | Result |
| --- | --- | --- | --- |
| Release-prep metadata points at the next public patch release. | The prep could accidentally reuse `1.13.10`/build `45`, conflict with the current tag, or update only one metadata source. | `make prepare-release VERSION=1.13.11 BUILD=46 NOTES=/tmp/foil-v1.13.11-release-notes.md` completed successfully. Diff inspection showed `CHANGELOG.md`, `package.json`, `package-lock.json`, `Foil.xcodeproj/project.pbxproj`, and `project.yml` updated to `1.13.11`/`46` where applicable. | PASS |
| Release helper defaults remain valid after version/build updates. | Version changes could break notarized QA/release helper defaults before signing/notarization. | `scripts/test-build-notarized-qa-dmg.sh` passed with `PASS: build-notarized-qa-dmg defaults`. | PASS |
| Release-prep branch is warning-clean and deterministic tests still pass. | The release metadata/changelog prep could hide project-file drift, warnings, or unit regressions. | `git diff --check` passed; `make build-warnings-as-errors` passed with `** BUILD SUCCEEDED **`; `make test` passed with `** TEST SUCCEEDED **` and xcresult `/Users/jeremywatt/Library/Developer/Xcode/DerivedData/Foil-ebuxbvgmspyykabsrfclwquwxbdi/Logs/Test/Test-Foil-2026.07.06_14-34-27--0700.xcresult`. | PASS |

Residual risk: release-prep changes still need PR review/merge queue, then tag `v1.13.11`, the Release workflow, public DMG/appcast/cask verification, production `/Applications` cask install, and fresh-user/TCC matrix proof before release readiness is complete.

## Release Readiness Verification: Installed-App Live Microphone QA Baseline

Date: 2026-07-07

Scope: first clean manual run for the new installed-app live microphone QA helper, using Mac mini 2 (`mm2`) after installing a fresh Notarized QA Build from current `main`.

| Claim | Strongest realistic failure mode | Evidence | Result |
| --- | --- | --- | --- |
| The installed-app live microphone helper proves the freshly installed QA artifact, not a stale `/Applications/Foil.app`. | The smoke could pass against an older public or QA build already installed on mm2, leaving the new helper and artifact unproved. | Notarized QA Build run `28897943936` succeeded for `main` at `1f1bfc13c3b0f553a4c0a836082b3e079a20fed3` and produced `Foil-1.13.11-28897943936-1f1bfc13c3b0f553a4c0a836082b3e079a20fed3-22-macos.dmg`; local artifact checksum matched `de6a53f46a10de35699e61277b92f41283f31dd21cfcb76d463a2d7431618c2d`, and the DMG passed Gatekeeper and stapler validation before install. On mm2, prior `/Applications/Foil.app` was backed up to `/tmp/foil-mm2-app-backups/20260707T205719Z-before-qa-1.13.11-28897943936`; installed `/Applications/Foil.app` reported bundle id `com.neonwatty.Foil`, version `1.13.11`, build `28897943936`, and passed deep `codesign` plus Gatekeeper as `Notarized Developer ID`. | PASS |
| The installed QA app can capture live microphone audio through the new helper. | The app could launch and report success while microphone permission, Apple voice playback, selected input, audio levels, or captured bytes are missing or stale. | `RUN_INSTALLED_LIVE_MICROPHONE_TESTS=1 HOST=mm2 APP_PATH=/Applications/Foil.app EXPECTED_VERSION=1.13.11 EXPECTED_BUILD=28897943936 scripts/run-installed-live-microphone-qa.sh` passed. mm2 evidence directory: `/tmp/foil-installed-live-microphone-qa-20260707T205733Z`. Receipt recorded `status=pass`, `microphone_permission_status=authorized`, `input_route_request=system-default`, available input `USB PnP Sound Device`, `apple_voice_process_started=true`, `apple_voice_exit_status=0`, `level_sample_count=43`, `level_peak=1.0000`, `file_level_peak=1.0000`, `bytes=141696`, and `elapsed_seconds=4.532`. | PASS |
| The helper handles the app-already-running case by cleaning up and relaunching the installed app. | A manual pass from a stopped app could hide stale-process, duplicate-instance, or cleanup/relaunch failures when Foil is already running. | Started `/Applications/Foil.app` on mm2 before the smoke; `/tmp/foil-installed-live-microphone-qa-20260707T-already-running-28897943936/preexisting-process.txt` recorded `preexisting_pid=60777` from `/Applications/Foil.app/Contents/MacOS/Foil`. Reran `RUN_INSTALLED_LIVE_MICROPHONE_TESTS=1 HOST=mm2 APP_PATH=/Applications/Foil.app EXPECTED_VERSION=1.13.11 EXPECTED_BUILD=28897943936 EVIDENCE_DIR=/tmp/foil-installed-live-microphone-qa-20260707T-already-running-28897943936 scripts/run-installed-live-microphone-qa.sh`; stderr shows the helper relaunched Foil as pid `60836`, and the final mm2 `pgrep -x Foil` check returned no running Foil process. Receipt recorded `status=pass`, `microphone_permission_status=authorized`, `apple_voice_process_started=true`, `level_sample_count=45`, `level_peak=1.0000`, `file_level_peak=1.0000`, `bytes=148096`, and `elapsed_seconds=4.718`. | PASS |
| Post-reboot installed-app live microphone QA passes after a clean desktop login. | The helper may depend on an active Aqua login session; immediately after reboot, SSH could return before the desktop user has logged in, causing LaunchServices to fail before the app can write a result receipt. | Pre-reboot mm2 reported boot time `Sat Jun 20 14:03:40 2026`, uptime `17 days`, version `1.13.11`, build `28897943936`, and no Foil process. Remote GUI restart via `osascript -e 'tell application "System Events" to restart'` succeeded; after SSH returned, `kern.boottime` was `Tue Jul 7 16:14:50 2026` and `/Applications/Foil.app` still reported version `1.13.11`, build `28897943936`. The first post-reboot attempt failed before receipt creation because the machine was still at loginwindow: LaunchServices reported `RBSRequestErrorDomain Code=5 "Launch failed"` with underlying `OSLaunchdErrorDomain Code=125 "Domain does not support specified action"`; inspection showed `console_user=root`, `kCGSessionLoginDoneKey=FALSE`, no `launchctl print gui/$(id -u)` domain, and `result_exists=no` under `/tmp/foil-installed-live-microphone-qa-20260707T-post-reboot-28897943936`. After operator console login, inspection showed `console_user=jeremywatt`, `kCGSessionLoginDoneKey=TRUE`, and GUI domain present. Rerunning `RUN_INSTALLED_LIVE_MICROPHONE_TESTS=1 HOST=mm2 APP_PATH=/Applications/Foil.app EXPECTED_VERSION=1.13.11 EXPECTED_BUILD=28897943936 EVIDENCE_DIR=/tmp/foil-installed-live-microphone-qa-20260707T-post-reboot-logged-in-28897943936 scripts/run-installed-live-microphone-qa.sh` passed. Receipt recorded `status=pass`, `microphone_permission_status=authorized`, `apple_voice_process_started=true`, `level_sample_count=43`, `level_peak=1.0000`, `file_level_peak=1.0000`, `bytes=141696`, and `elapsed_seconds=4.540`; final `pgrep -x Foil` returned no running Foil process. | PASS AFTER CONSOLE LOGIN |
| The helper stays green after the next QA artifact install. | Prior passes could have been tied to one installed build; a later QA artifact install might break signing, install identity, LaunchServices launch, or live microphone capture. | Dispatched Notarized QA Build run `28906650156` for `main` at `1f1bfc13c3b0f553a4c0a836082b3e079a20fed3`; run completed successfully and uploaded `Foil-1.13.11-28906650156-1f1bfc13c3b0f553a4c0a836082b3e079a20fed3-23-macos.dmg`. Downloaded artifact checksum matched `fdcda05a222506a3fd3c77c260b30b896db2ee52e859bcd7c685daf08b9c2bd5`, the DMG passed Gatekeeper and stapler validation, and the mounted app reported bundle id `com.neonwatty.Foil`, version `1.13.11`, build `28906650156`, Developer ID Gatekeeper acceptance, and deep strict codesign pass. On mm2, `/Applications/Foil.app` build `28897943936` was backed up to `/tmp/foil-mm2-app-backups/20260707T2350-before-qa-1.13.11-28906650156`, replaced with build `28906650156`, then passed Gatekeeper and deep codesign. `RUN_INSTALLED_LIVE_MICROPHONE_TESTS=1 HOST=mm2 APP_PATH=/Applications/Foil.app EXPECTED_VERSION=1.13.11 EXPECTED_BUILD=28906650156 EVIDENCE_DIR=/tmp/foil-installed-live-microphone-qa-20260707T-next-artifact-28906650156 scripts/run-installed-live-microphone-qa.sh` passed. Receipt recorded `status=pass`, `microphone_permission_status=authorized`, `apple_voice_process_started=true`, `level_sample_count=44`, `level_peak=1.0000`, `file_level_peak=1.0000`, `bytes=144896`, and `elapsed_seconds=4.708`; final `pgrep -x Foil` returned no running Foil process. | PASS |
| Manual GitHub workflow dispatch path is ready for review. | Adding a workflow could omit required inputs, fail to preserve the console-login prerequisite, fail to collect remote evidence, or drift from the proven installed-app helper. | Added `.github/workflows/installed-live-microphone-qa.yml` as a manual `workflow_dispatch` only. Inputs cover SSH host, app path, expected version/build, input route, duration, Apple voice text, and cleanup behavior. The workflow uses the same trusted macOS GUI runner labels as `Live Microphone QA`, proves the remote GUI session before launch, runs `scripts/run-installed-live-microphone-qa.sh`, collects the remote `/tmp/foil-installed-live-microphone-qa-${{ github.run_id }}` evidence directory into an `installed-live-microphone-qa` artifact, and keeps merge-queue/webhook triggers out of scope. Updated `docs/microphone-qa.md` to document the manual dispatch and active desktop-login prerequisite. Local validation passed: `scripts/test-run-installed-live-microphone-qa.sh`, Ruby YAML parse for the new workflow and existing `live-microphone-qa.yml`, and `git diff --check`. | LOCAL PASS / DISPATCH PENDING |

Residual risk: manual stability gates are now green enough to review and land the manual workflow dispatch. The new workflow has not been dispatched from GitHub yet because it is still a local change; run one manual workflow dispatch after it is available on the default branch. Keep merge-queue-tip or webhook automation out of scope until that manual workflow path proves stable.

## Release Readiness Verification: Public Release `v1.13.11`

Date: 2026-07-06

Scope: public release, appcast, Homebrew cask, `/Applications` install, and packaged route-resolution smoke for Foil `1.13.11` build `46` after PR #361 merged at `547a2fbf0041d7c904c35ff18658aa0f8ed43a33`.

| Claim | Strongest realistic failure mode | Evidence | Result |
| --- | --- | --- | --- |
| Release-prep PR merged through the required queue before tagging. | The public tag could be cut from an unqueued branch or a merge commit without required checks. | PR #361 (`codex/prepare-v1.13.11-release`) merged at `2026-07-06T21:53:07Z` with merge commit `547a2fbf0041d7c904c35ff18658aa0f8ed43a33`. Merge-group checks passed: Local macOS E2E run `28825386926`, E2E Groq run `28825387016`, E2E OpenAI run `28825387245`, and CI run `28825387040` including Build, Unit Tests, Audio UX Snapshots, Focused UI Smoke, and CI Gate. Post-merge `main` checks passed: CodeQL run `28825811504` and Local macOS E2E run `28825811784`. | PASS |
| Public Release workflow produced the expected release assets from the merge commit. | A release could publish from the wrong commit, fail notarization, omit assets, or upload mismatched metadata. | Tag `v1.13.11` points at `547a2fbf0041d7c904c35ff18658aa0f8ed43a33`. Release workflow run `28825897802` succeeded, including tag checkout verification, GitHub release creation/update, Developer ID signing, DMG build/notarization/upload, notarization validation, appcast generation/upload, Homebrew cask update, and keychain cleanup. Release `v1.13.11` is published, not draft, not prerelease, and includes `Foil-1.13.11-macos.dmg`, `Foil-1.13.11-macos.dmg.sha256`, and `appcast.xml`. | PASS |
| Public DMG checksum, signing, and notarization are valid. | The DMG could be corrupt, unsigned, unstapled, or different from the checksum and cask. | Downloaded release assets to `/tmp/foil-release-1.13.11`. Local `shasum -a 256 Foil-1.13.11-macos.dmg` and the `.sha256` asset both reported `4805e6ca60d7bc8597673dbbee89289997363a8c4fe2e0b6c428c6d245fecd57`. `spctl -a -vv -t open --context context:primary-signature` accepted the DMG as `source=Notarized Developer ID`, origin `Developer ID Application: Mean Weasel LLC (B3A6AN2HA4)`; `xcrun stapler validate` passed. | PASS |
| Mounted release app has the expected signed identity. | The DMG could be trusted while the contained app has the wrong bundle id, version/build, Sparkle key, or signing identity. | Mounted read-only DMG contained `.background/dmg-background.png`, `.DS_Store`, `Applications`, and `Foil.app`. The mounted app reported bundle id `com.neonwatty.Foil`, version `1.13.11`, build `46`, and a 32-byte `SUPublicEDKey`; `spctl -a -vv -t execute /Volumes/Foil/Foil.app` accepted it as Notarized Developer ID; `codesign --verify --deep --strict --verbose=2` passed; `codesign -dv --verbose=4` reported Developer ID Application authority, team `B3A6AN2HA4`, and hardened runtime. | PASS |
| Sparkle appcast points at the correct public asset. | Appcast could point at the wrong DMG, wrong version/build, malformed XML, missing signature, or stale length. | `xmllint --noout /tmp/foil-release-1.13.11/appcast.xml` passed. Appcast title is `Version 1.13.11`, `sparkle:version` is `46`, `sparkle:shortVersionString` is `1.13.11`, minimum system version is `14.0`, enclosure URL is `https://github.com/usefoil/foil/releases/download/v1.13.11/Foil-1.13.11-macos.dmg`, length is `3336613`, and `sparkle:edSignature` is non-empty. | PASS |
| Public Homebrew cask points at the release DMG and matching SHA. | Local Homebrew cache or remote tap could still point at `1.13.10`, proving the wrong artifact. | Remote `mean-weasel/homebrew-foil` cask contains version `1.13.11`, URL `https://github.com/usefoil/foil/releases/download/v1.13.11/Foil-1.13.11-macos.dmg`, and sha256 `4805e6ca60d7bc8597673dbbee89289997363a8c4fe2e0b6c428c6d245fecd57`. A first local `brew info` still showed cached `1.13.10`; after `brew update`, `brew info --cask mean-weasel/foil/foil` showed `1.13.11` and `brew fetch --cask --force mean-weasel/foil/foil` fetched `Cask foil (1.13.11)`. | PASS AFTER CACHE REFRESH |
| Temp-dir public cask extraction verifies the shipped cask app identity. | The cask metadata could be correct but extraction could produce the wrong app or a signing/Gatekeeper failure. | After `brew update`, `REQUIRED_COMMIT=547a2fbf0041d7c904c35ff18658aa0f8ed43a33 make check-production-permissions-cask` passed with 0 warnings. It verified latest release `v1.13.11`, required commit inclusion, public cask `1.13.11`, temp extraction as Foil `1.13.11` build `46`, bundle id `com.neonwatty.Foil`, microphone usage description, 32-byte `SUPublicEDKey`, Developer ID authority/team `B3A6AN2HA4`, Gatekeeper `Notarized Developer ID`, and deep strict codesign. | PASS |
| `/Applications/Foil.app` production install launches and passes setup helper checks. | A valid cask/DMG could still fail from the canonical production path because of stale Homebrew cache, quarantine, LaunchServices, or path-specific App Management state. | `brew upgrade --cask mean-weasel/foil/foil` upgraded `1.13.10 -> 1.13.11`, but the first `/Applications/Foil.app` launch stalled before `main`: `sample` showed only `_dyld_start`, 96 KB footprint, and `xattr` showed bundle-wide `com.apple.quarantine`. Removing quarantine was denied with `Operation not permitted`. The same release app copied from the verified DMG to `/tmp/foil-release-launch-test/Foil.app` launched past dyld. A differently named `/Applications/FoilLaunchTest.app` also launched; replacing `/Applications/Foil.app` by renaming that known-good copy into the canonical path fixed the path-state failure. Final `/Applications/Foil.app` reports version `1.13.11` build `46`, has no `com.apple.quarantine`, passes `spctl`, deep strict `codesign`, and `make guide-production-permissions-qa` with 0 warnings and active process path `/Applications/Foil.app/Contents/MacOS/Foil`. | PASS AFTER LOCAL PATH REMEDIATION |
| Public installed release app proves the packaged cleanup route-resolution repair. | Earlier route proof could have covered only the QA artifact, leaving the public release build still resolving Messages to the default cleanup group. | Launched `/Applications/Foil.app` `1.13.11` build `46` with `FOIL_UITEST_STATE_PATH=/tmp/foil-public-release-route-resolution-1.13.11/state.json`, `FOIL_UITEST_COMMAND_PATH=/tmp/foil-public-release-route-resolution-1.13.11/command.json`, `--ui-testing --seed-usage-events --show-app-shell --settings-tab-cleanup`, then sent app command `resolveCleanupGroup` for `displayName=Messages (iMessage)` and `bundleIdentifier=com.apple.MobileSMS`. State snapshot recorded `cleanupGroupResolution: displayName=Messages (iMessage) bundleIdentifier=com.apple.MobileSMS appPath= groupID=A70168CA-00A9-43DE-A91B-F4D4F5F76C00 groupName=New group isDefault=false appMatcherCount=1 processingMode=raw providerID=none`. Screenshot: `/tmp/foil-public-release-route-resolution-1.13.11/app-shell-route-resolution.png`. | PASS |

Residual risk: fresh-user/fresh-machine setup and TCC matrix rows were not rerun for `v1.13.11` on a disposable macOS account, VM, spare Mac, or erased machine. This daily-driver account retained prior Accessibility/Microphone history and exposed a local `/Applications/Foil.app` path-state issue during repeated QA replacements; keep that as operator-owned follow-up rather than treating this release log as fresh-user proof.

## Release Readiness Verification: `v1.13.11` Fresh-User/TCC Matrix Packet

Date: 2026-07-06

Scope: T013 fresh-user/TCC setup proof for Foil `v1.13.11` build `46`. This section intentionally separates non-fresh local sanity evidence from the still-required disposable fresh-user matrix.

| Claim | Strongest realistic failure mode | Evidence | Result |
| --- | --- | --- | --- |
| The installed production app is still the intended public build before any fresh-user run is attempted. | Local `/Applications/Foil.app` could have drifted back to an old QA build or stale cask install after the release work. | `/Applications/Foil.app` reports bundle id `com.neonwatty.Foil`, version `1.13.11`, build `46`. `brew info --cask mean-weasel/foil/foil` reports cask `1.13.11`. `gh release view v1.13.11` reports a published, non-draft, non-prerelease release with `Foil-1.13.11-macos.dmg`, `.sha256`, and `appcast.xml`; the DMG asset digest is `sha256:4805e6ca60d7bc8597673dbbee89289997363a8c4fe2e0b6c428c6d245fecd57`. | PASS |
| The installed production app still satisfies macOS trust checks. | A local path-state or quarantine issue could leave the canonical app untrusted even though the release asset is valid. | `spctl -a -vv -t execute /Applications/Foil.app` accepted the app as `source=Notarized Developer ID`, origin `Developer ID Application: Mean Weasel LLC (B3A6AN2HA4)`. `codesign --verify --deep --strict --verbose=2 /Applications/Foil.app` passed for the app and embedded Sparkle components. | PASS |
| This daily-driver account can be used as fresh-user proof. | Retained app defaults, Keychain state, or TCC grants could make first-run setup appear ready before a real new user grants permissions. | Freshness was disproven. `defaults export com.neonwatty.Foil -` shows `SUHasLaunchedBefore=true`; diagnostics already show `SetupHealth: accessibilityTrusted=true`, `SetupHealth: microphone=authorized`, `SetupHealth: inputDevices count=3 selectedUID=systemDefault microphoneState=ready`, and `KeychainHelper: read status=success`. Therefore this account can only provide non-fresh sanity evidence. | BLOCKED |
| Fresh-user/TCC matrix is ready for execution by an operator. | The real run could be under-specified and produce ambiguous screenshots or logs that cannot close the release gap. | Use `docs/fresh-machine-homebrew-onboarding-smoke.md` against `v1.13.11` build `46` in a disposable macOS user, VM, spare Mac, or freshly erased machine. Required rows: fresh install; Accessibility already granted; Accessibility granted while onboarding is open; Microphone prompt grant; Microphone already granted; permission revoked while running; quit/relaunch persistence. Record macOS version, architecture, install commands, version/build, `spctl`, `codesign`, screenshots or visible state notes, diagnostics, and any follow-up issue links. | READY / EXTERNAL ENV REQUIRED |

Operator packet for the external run:

```sh
brew untap mean-weasel/foil || true
brew tap mean-weasel/foil https://github.com/mean-weasel/homebrew-foil
brew info --cask mean-weasel/foil/foil
brew install --cask mean-weasel/foil/foil
/usr/libexec/PlistBuddy -c 'Print :CFBundleIdentifier' /Applications/Foil.app/Contents/Info.plist
/usr/libexec/PlistBuddy -c 'Print :CFBundleShortVersionString' /Applications/Foil.app/Contents/Info.plist
/usr/libexec/PlistBuddy -c 'Print :CFBundleVersion' /Applications/Foil.app/Contents/Info.plist
spctl -a -vv -t execute /Applications/Foil.app
codesign --verify --deep --strict --verbose=2 /Applications/Foil.app
```

Optional Foil-scoped resets are allowed only inside that disposable environment:

```sh
tccutil reset Accessibility com.neonwatty.Foil
tccutil reset ListenEvent com.neonwatty.Foil
tccutil reset Microphone com.neonwatty.Foil
```

Residual risk: the fresh-user/TCC matrix remains open until those rows are run outside this daily-driver account and the results are appended to this log or issue #154.

## Release Readiness Verification: `v1.13.11` Post-Merge Local E2E Route-Settle Repair

Date: 2026-07-06

Scope: post-merge `main` Local macOS E2E failure after PR #363 merged at `7b89f45d33112e273b35822b4e01650189cf7dcd`.

| Claim | Strongest realistic failure mode | Evidence | Result |
| --- | --- | --- | --- |
| The fresh-user/TCC packet merge itself passed required queue checks. | The packet could have merged without the required merge-group proof. | PR #363 merged at `2026-07-06T23:16:30Z` with merge commit `7b89f45d33112e273b35822b4e01650189cf7dcd`. Merge-group runs passed for Local macOS E2E `28829437689`, E2E OpenAI `28829437622`, E2E Groq `28829437623`, and CI `28829437614`; CI included Focused UI Smoke in `8m2s` and CI Gate. | PASS |
| Post-merge `main` Local macOS E2E remained green. | A runner-specific audio route state could fail only after merge, leaving `main` with a red required signal. | Post-merge push Local macOS E2E run `28829825202` failed, and rerun job `85501413239` reproduced the same failure. Logs showed default input `Jeremy's AirPods`, Foil switching to `USB PnP Sound Device`, `RecordingController.startRecording: waiting for input route settle ns=500000000`, then `E2E: stopping simulated recording` about `210ms` later and `RecordingController.stopRecording: cancelling pending recording start`. | FAIL / REPRODUCED |
| The repair makes the E2E harness wait past the route-settle window before stopping. | The harness could still stop during route settle, masking a live transcription failure as a timeout. | Patch increases the `--e2e-transcribe` simulated recording hold from `200ms` to `800ms`, exceeding the production `500ms` input-route settle. Focused proof: `xcodebuild test -scheme Foil -configuration Debug -destination 'platform=macOS' -only-testing:FoilTests/RecordingControllerMockTests -enableCodeCoverage NO` passed 14 tests, including new `testStopAfterDefaultInputRouteSettleStopsAudioRecorder`; the existing `testStopDuringDefaultInputRouteSettleCancelsAudioRecorderStart` still proves the prior failure mode. | LOCAL PASS / CI PENDING |

Residual risk: live post-merge Local macOS E2E must pass again on the self-hosted Mac after this repair lands; local focused tests prove the route-settle timing contract but do not call the live Groq API.

## Release Readiness Verification: Public Cask Gate For `e02b815`

Date: 2026-07-06

Scope: public Homebrew/cask verification for the intended release commit `e02b81564f08bc104c9fae5737eadbb45d629200`, without installing or replacing `/Applications/Foil.app`.

| Claim | Strongest realistic failure mode | Evidence | Result |
| --- | --- | --- | --- |
| Public cask points to an artifact containing `e02b815`. | The cask is valid but still points to the older `v1.13.10` release. | `gh api repos/mean-weasel/homebrew-foil/contents/Casks/foil.rb --jq '.content' | base64 --decode` showed cask version `1.13.10`, URL `https://github.com/usefoil/foil/releases/download/v1.13.10/Foil-1.13.10-macos.dmg`, and SHA-256 `1d025a1fb0cecabd472df6e986fc0b6555635f98de1f2a850030597a21a5d0f6`. `brew info --cask mean-weasel/foil/foil` also reported `1.13.10`. | BLOCKED |
| Temp-dir cask extraction verifies the currently public artifact identity. | The old public cask might also be malformed or untrusted, which would confuse release diagnosis. | `REQUIRED_COMMIT=e02b81564f08bc104c9fae5737eadbb45d629200 make check-production-permissions-cask` failed as expected on required commit inclusion, then extracted the public `1.13.10` cask into `/tmp/foil-release-apps` and verified bundle id `com.neonwatty.Foil`, version `1.13.10`, build `45`, `SUPublicEDKey`, Developer ID authority/team `B3A6AN2HA4`, Gatekeeper `Notarized Developer ID`, and deep strict codesign. | OLD CASK PASS / TARGET COMMIT BLOCKED |

Required follow-up: publish or produce an approved artifact containing `e02b815`, then update/verify the public cask. Do not install or use the current public cask as release proof for the merged feature stack.

## Release Readiness Verification: Setup/TCC Gate For `e02b815`

Date: 2026-07-06

Scope: read-only installed-app/setup inspection. No production reinstall, app replacement, or TCC reset was performed.

| Claim | Strongest realistic failure mode | Evidence | Result |
| --- | --- | --- | --- |
| Current `/Applications/Foil.app` can serve as setup/TCC proof for `e02b815`. | The installed app is valid and ready, but it is still the older public build, so setup/TCC proof would cover the wrong binary. | `/Applications/Foil.app` reports version `1.13.10`, build `45`, bundle id `com.neonwatty.Foil`; `codesign -dv --verbose=4 /Applications/Foil.app` reports Developer ID Application authority, team `B3A6AN2HA4`, and hardened runtime. `pgrep`/`ps` shows pid `19814` running from `/Applications/Foil.app/Contents/MacOS/Foil`. | BLOCKED FOR TARGET COMMIT |
| Existing diagnostics show the current installed app is setup-ready. | Diagnostics could show missing setup readiness, which would complicate later production proof. | Tail inspection of `~/Library/Application Support/Foil/Diagnostics/foil.log` showed `SetupHealth: accessibilityTrusted=true`, `SetupHealth: microphone=authorized`, `SetupHealth: inputDevices count=3 selectedUID=systemDefault microphoneState=ready`, Keychain read success, and API key state refreshed at `2026-07-06T16:55:34Z`. | OLD APP READY / TARGET COMMIT BLOCKED |

Post-QA-artifact update: Notarized QA Build run `28811180245` produced a verified DMG for `e02b815`, but the setup/TCC runbook still requires proof from the production `/Applications/Foil.app` bundle or a disposable fresh environment. The current production app is running as the older `1.13.10` build `45`; no approval has been given to quit, replace, launch a duplicate same-bundle app, reset TCC, or mutate this user's permissions.

Post-approval update: operator approved replacing `/Applications/Foil.app` with the QA artifact. The old app was backed up to `/tmp/foil-app-backups/20260706-105355-Foil-1.13.10-build45.app`; the verified QA DMG was mounted read-only and copied into `/Applications/Foil.app`. The installed app now reports bundle id `com.neonwatty.Foil`, version `1.13.10`, build `28811180245`; `spctl -a -vv -t execute /Applications/Foil.app` accepted it as `Notarized Developer ID`; `codesign --verify --deep --strict --verbose=2 /Applications/Foil.app` passed. `make guide-production-permissions-qa` passed with 0 warnings and active process path `/Applications/Foil.app/Contents/MacOS/Foil`. Quit/relaunch persistence passed: relaunch pid `88469` ran from `/Applications/Foil.app/Contents/MacOS/Foil`, diagnostics showed `SetupHealth: accessibilityTrusted=true`, `SetupHealth: microphone=authorized`, `SetupHealth: inputDevices count=3 selectedUID=systemDefault microphoneState=ready`, and `HotkeyMonitor: permission refresh start succeeded`.

Post-route-resolution QA update: operator approved commit/push/QA artifact/install for the packaged route-resolution repair. Branch `codex/packaged-route-resolution-qa` commit `c042651b97b27690547a055307db378563708fa4` was pushed and draft PR #360 was opened. Notarized QA Build run `28817029558` checked out `origin/codex/packaged-route-resolution-qa` at `c042651b97b27690547a055307db378563708fa4`, produced `Foil-1.13.10-28817029558-codex-packaged-route-resolution-qa-17-macos.dmg`, and uploaded checksum `92a4ae44fbba01ea2ce509d40d1428ec92c1e60a9deaf01fccab90ee86fc4166`, matching the downloaded DMG. `spctl -a -vv -t open --context context:primary-signature` accepted the DMG as `Notarized Developer ID`; `xcrun stapler validate` passed; the mounted app reported bundle id `com.neonwatty.Foil`, version `1.13.10`, build `28817029558`, a 32-byte `SUPublicEDKey`, Gatekeeper `Notarized Developer ID`, and deep strict codesign pass. `/Applications/Foil.app` build `28811180245` was backed up to `/tmp/foil-app-backups/20260706-122138-before-route-resolution-qa/Foil.app`, replaced with the verified build `28817029558`, and then passed `spctl`, `codesign --verify --deep --strict`, and `make guide-production-permissions-qa` with 0 warnings. Normal launch pid `60556` ran from `/Applications/Foil.app/Contents/MacOS/Foil`; diagnostics at `2026-07-06T19:23:07Z` showed `SetupHealth: accessibilityTrusted=true`, `SetupHealth: microphone=authorized`, `SetupHealth: inputDevices count=3 selectedUID=systemDefault microphoneState=ready`, Keychain read success, and `HotkeyMonitor: start succeeded`.

Required follow-up: fresh-user/TCC-reset rows still require a disposable environment or explicit approval for scoped TCC resets. Provider/API setup was not reprovisioned in this daily-driver account; diagnostics reported Keychain `itemNotFound` after the QA replacement.

## Release Readiness Verification: Packaged Feature Smoke For `e02b815`

Date: 2026-07-06

Scope: packaged cleanup groups / usage insights / recent-app assignment smoke gate.

| Claim | Strongest realistic failure mode | Evidence | Result |
| --- | --- | --- | --- |
| Packaged app proves cleanup groups, Usage Insights, and recent-app assignment for `e02b815`. | Running the smoke against the existing installed app would prove `1.13.10` build `45`, not the newly merged feature stack. | Artifact gate found no public release or Notarized QA Build artifact for `e02b815`; setup/TCC gate confirmed `/Applications/Foil.app` is still `1.13.10` build `45`. | BLOCKED |
| Verified QA artifact can be smoked safely without disturbing the daily-driver app. | Launching the mounted QA app could activate/compete with the currently running production app or require terminating/replacing it; UI-test smoke also drives desktop UI. | Source inspection shows `--ui-testing` uses isolated temp history/usage storage in `FoilApp.swift`, but same-bundle duplicate launches normally hit `SingleInstanceGuard`; `pgrep`/`ps` previously showed `/Applications/Foil.app` running as build `45`; no approval exists to quit, replace, or drive the production app session. | BLOCKED PENDING APPROVAL |
| Installed QA app launches the packaged app shell and seeded usage metadata path. | The package might install and launch, but the merged usage Insights data path could be absent or write to real user data. | After approved replacement, `open -n /Applications/Foil.app --args --ui-testing --reset-defaults --seed-usage-events --show-app-shell` launched `/Applications/Foil.app/Contents/MacOS/Foil --ui-testing --reset-defaults --seed-usage-events --show-app-shell`. The UI-test state file under `/var/folders/.../T/foil-ui-tests-state.json` reported `Ready` for status, Accessibility, Microphone, API Key, and `canStartRecording:true`. The packaged app wrote `/var/folders/.../T/FoilUITests/usage/usage-events.json` with 8 events, 710 total words, and source apps `Messages (iMessage)`, `Codex`, `Google Chrome`, `Ghostty`, `Terminal`, and `Mail`. Screenshot `/tmp/foil-packaged-smoke-e02b815/app-shell-home.png` shows the installed QA app shell rendered with setup health ready. | PARTIAL PASS |
| Packaged UI navigation proves Insights, recent-app picker, cleanup-group assignment, and later routing. | The package may have seeded data, but the visible Insights/Cleanup workflow could be inaccessible or fail when driven in the packaged app. | Repeated `cliclick` coordinate attempts against the visible SwiftUI sidebar left the app shell on Home; System Events exposed only the top `AXHostingView` and did not expose actionable nested SwiftUI buttons for direct AX selection. Screenshots `insights-click-final.png` and `cleanup-click-final.png` still show Home. No cleanup group was assigned and no later routing proof was produced. | BLOCKED |
| GoalBuddy rerun proves packaged Usage Insights navigation. | The seeded usage file may exist, but the visible installed app might still fail to render Insights. | With `/Applications/Foil.app` build `28811180245`, `launchctl setenv FOIL_UITEST_STATE_PATH /tmp/foil-packaged-smoke-e02b815-goalbuddy/state.json`, `defaults write com.neonwatty.Foil FoilAppShell.pendingSelection insights`, then `open -n /Applications/Foil.app --args --ui-testing --reset-defaults --seed-usage-events --show-app-shell` produced a Ready state snapshot and screenshot `/tmp/foil-packaged-smoke-e02b815-goalbuddy/insights-pending-selection.png`. The screenshot shows visible Insights with 710 total words, 8 sessions, 17 min, Daily Trend, and top apps Messages (iMessage), Google Chrome, Terminal, Mail, and Ghostty. | PASS |
| GoalBuddy rerun proves packaged Cleanup recent-app assignment persists across relaunch in the isolated UI-test suite. | Assignment might appear in memory only, fail to persist, or accidentally mutate the production defaults domain. | With `/Applications/Foil.app` build `28811180245`, `defaults write com.neonwatty.Foil FoilAppShell.pendingSelection cleanup` plus `open -n /Applications/Foil.app --args --ui-testing --reset-defaults --seed-usage-events --show-app-shell` opened Cleanup (`cleanup-open-before-add-group.png`). Coordinate click using live app-shell window geometry `pos=385,136 size=940x668` created a non-default group (`cleanup-open-after-add-group.png`) and assigned Messages (iMessage) from recent apps (`cleanup-open-after-add-messages.png`). After quit/relaunch with UI-test isolation, screenshot `cleanup-after-relaunch-persistence.png` still shows `New group` with `Raw - 1 apps`. Decoding `defaults export com.neonwatty.Foil.UITests -` shows `cleanupGroups` contains non-default `New group` with matcher `bundleIdentifier:"com.apple.MobileSMS"`, `displayName:"Messages (iMessage)"`; `defaults export com.neonwatty.Foil -` remained at the default group only. | PASS |
| GoalBuddy rerun proves later cleanup routing uses the assigned group. | The assignment may persist, but the packaged app may still resolve recordings to the default group when the source app is Messages. | Not proven by the current packaged app. Source inspection found `AppState.resolveCleanupGroup(for:)`, but the installed build's UI-test state snapshot and command relay do not expose a packaged runtime route-resolution receipt, and diagnostics contain no cleanup-group routing log for this workflow. | BLOCKED / TESTABILITY GAP |
| Local repair exposes a packaged-compatible cleanup route-resolution receipt. | The repair might compile but still fail to prove that a supplied app context resolves to the assigned group. | Added UI-test app command `resolveCleanupGroup` in `Foil/UITestingController.swift`; it records `cleanupGroupResolution` in the existing state snapshot with display name, bundle id, resolved group id/name, default flag, matcher count, processing mode, and provider. Updated `FoilUITests/FoilUITests.swift` to assert Messages resolves to `New group`. `xcodebuild build-for-testing ...` passed. Focused resolver tests passed. Direct Debug-app command-file proof returned `groupName=New group isDefault=false appMatcherCount=1 processingMode=raw providerID=none` for `bundleIdentifier=com.apple.MobileSMS`. The full UI test could not run in this desktop session because Xcode timed out enabling automation mode before assertions. | LOCAL PASS / PACKAGED ARTIFACT REQUIRED |
| Installed route-resolution QA artifact proves later cleanup routing uses the assigned group. | The repair could exist only in source/Debug, the signed artifact could be built from the wrong ref, or the installed app could still resolve Messages to the default group. | Notarized QA Build run `28817029558` checked out `origin/codex/packaged-route-resolution-qa` at `c042651b97b27690547a055307db378563708fa4`; `/Applications/Foil.app` was replaced with the verified build `28817029558`. The isolated `com.neonwatty.Foil.UITests` defaults decoded `cleanupGroups` with non-default `New group` matcher `bundleIdentifier:"com.apple.MobileSMS"`, `displayName:"Messages (iMessage)"`. Launching `/Applications/Foil.app --ui-testing --seed-usage-events --show-app-shell` with `FOIL_UITEST_STATE_PATH=/tmp/foil-packaged-smoke-route-resolution-28817029558/state.json` and `FOIL_UITEST_COMMAND_PATH=/tmp/foil-packaged-smoke-route-resolution-28817029558/command.json`, then sending app command `resolveCleanupGroup` for Messages, recorded `cleanupGroupResolution: displayName=Messages (iMessage) bundleIdentifier=com.apple.MobileSMS appPath= groupID=A70168CA-00A9-43DE-A91B-F4D4F5F76C00 groupName=New group isDefault=false appMatcherCount=1 processingMode=raw providerID=none`. Screenshot: `/tmp/foil-packaged-smoke-route-resolution-28817029558/app-shell-route-resolution.png`. | PASS |

Route-resolution follow-up status: satisfied by Notarized QA Build run `28817029558` and installed packaged smoke above. Remaining release-readiness follow-ups are outside this packaged feature-smoke row: public release/cask update, fresh-user or scoped TCC-reset proof, and any provider/live-credential gates required by the broader release board.

## Setup Permission Release Gate

Copy this table into the active release section and fill every row before
declaring a release setup-permission ready. Use the production
`/Applications/Foil.app` installed from `brew install --cask mean-weasel/foil/foil`
unless the row explicitly uses a temporary app directory.

| Gate | Command / steps | Expected result | Result | Evidence / notes |
| --- | --- | --- | --- | --- |
| Public cask identity smoke | `REQUIRED_COMMIT=<release-required-commit> make check-production-permissions-cask` | Latest release contains the required commit, public cask installs into `/tmp/foil-release-apps`, version/build match release, bundle id is `com.neonwatty.Foil`, Gatekeeper reports `Notarized Developer ID`, and codesign passes. | PENDING | |
| `/Applications` production install | Uninstall or move aside any previous Foil only with operator approval, then `brew install --cask mean-weasel/foil/foil`; run `make guide-production-permissions-qa`. | Previous version is gone or explicitly backed up, active process path is `/Applications/Foil.app/Contents/MacOS/Foil`, helper reports Developer ID signing, `Notarized Developer ID`, and privacy panes open. | PENDING | |
| Fresh install onboarding | Use a fresh/disposable account, VM, or spare Mac. If approved in that environment only, reset Foil TCC with `tccutil reset Accessibility com.neonwatty.Foil`, `tccutil reset ListenEvent com.neonwatty.Foil`, and `tccutil reset Microphone com.neonwatty.Foil`. Install with Homebrew and launch. | First-run setup appears and no row incorrectly starts as stale `Ready`. | PENDING | |
| Accessibility already granted | Grant Accessibility for `/Applications/Foil.app`, quit/relaunch, then navigate to the Accessibility onboarding step. | The step shows `Ready`; `Enable Accessibility` is absent. Diagnostics include `SetupHealth: accessibilityTrusted=true`. | PENDING | |
| Accessibility grant while onboarding is open | Start with Accessibility disabled, stay on the Accessibility step, enable Foil in System Settings, return to Foil or relaunch if macOS requires it. | The step updates to `Ready` and stops showing `Enable Accessibility`. | PENDING | |
| Microphone prompt grant while onboarding is open | Start with Microphone not determined, stay on the Microphone step, trigger the in-app microphone action, and grant the macOS prompt. | The step updates to `Ready`; final `Get Started` becomes enabled once provider/API setup is also ready. | PENDING | |
| Microphone already granted | Grant Microphone before reaching the Microphone step, then relaunch and navigate there. | The step shows `Ready`; final `Get Started` is enabled when Accessibility and provider/API setup are ready. Diagnostics show `SetupHealth: microphone=authorized`. | PENDING | |
| Permission revoked while app is running | From ready state, revoke Accessibility or Microphone in System Settings, return to Foil, run Test Setup, then re-enable the permission. | Setup moves to an actionable non-ready state, then returns to `Ready`; `Get Started` is disabled while a required permission is missing. | PENDING | |
| Quit/relaunch persistence | Complete Accessibility, Microphone, and provider setup; quit and relaunch `/Applications/Foil.app`. | Setup remains `Ready`; onboarding does not re-block on stale permission text. | PENDING | |

## Release Candidate

- Version: `v1.8.2`
- Commit: `fe4da4e`
- Date: 2026-05-11 09:01:15 MST
- Runner: `jeremywatt`
- macOS version: 26.3.1 (a), build 25D771280a
- Xcode version: Xcode 26.3, build 17C529

## Automated Gates

| Gate | Command | Result | Artifact / notes |
| --- | --- | --- | --- |
| Build with warnings as errors | `xcodebuild build -scheme Foil -configuration Debug -destination 'platform=macOS' OTHER_SWIFT_FLAGS='-warnings-as-errors'` | PASS | `** BUILD SUCCEEDED **` on 2026-05-11 09:01 MST. |
| Unit XCTest suite | `make test` | PASS | xcresult: `Test-Foil-2026.05.11_09-01-41--0700.xcresult`; `** TEST SUCCEEDED **`. |
| UI XCTest suite | `make test-ui` | PASS | xcresult: `Test-Foil-2026.05.11_09-01-56--0700.xcresult`; `** TEST SUCCEEDED **`. |
| Installed app real paste | `make test-paste-real` | SKIP RECORDED | Default run exited `2` on AX-window skip as expected; explicit rerun with `ALLOW_LOCAL_QA_SKIP=1` exited `0`. Production `insertAsync` path was exercised and UI-test bypass was not used, but target AX window was not exposed to the installed app process. |
| Cross-app paste | `make test-cross-app` | PASS WITH SKIPS | TextEdit async paste PASS; SkyLight background paste PASS; Terminal PASS; Chrome PASS; VS Code skipped because app is not installed; Notes skipped to avoid mutating persistent Notes data. |
| Live cleanup quality | `GROQ_API_KEY=... make test-cleanup-quality` | BLOCKED | `make test-cleanup-quality` without `GROQ_API_KEY` failed fast as intended. Rerun with a valid key before release sign-off. |
| Live audio cleanup quality | `env -u GROQ_API_KEY -u OPENAI_API_KEY make test-live-audio-cleanup-quality` | BLOCKED | The new Apple voice audio cleanup harness generated local fixtures, then failed at the live transcription credential gate with `GROQ_API_KEY or E2E_API_KEY is required`. Rerun with a valid provider key before release sign-off. |
| Live audio cleanup quality with Apple voice fixtures | `source ./.env`; `E2E_ARTIFACT_DIR=tests/fixtures/audio-cleanup-quality/generated-live-default E2E_CLEANUP_TIMEOUT_SECONDS=60 make test-live-audio-cleanup-quality` | PASS | Used local `.env` provider keys without printing values. Generated four Apple voice WAV fixtures, transcribed through Groq `whisper-large-v3-turbo`, defaulted cleanup to OpenAI `gpt-5.4-mini`, and wrote pass-status artifacts for filler/stutter, false start, obvious structure, and vocabulary fixtures. Secret scan of generated artifacts returned no matches. |
| Semantic release dry run | `npx semantic-release --dry-run --no-ci` | PASS / NO RELEASE BEFORE TRIGGER | First run failed without `GH_TOKEN`; rerun with `GH_TOKEN="$(gh auth token)"` passed. semantic-release found 3 commits since `v1.8.1`, none release-triggering. PR #48 then added an intentional `fix:` release trigger and published `v1.8.2`. |

## Local-Only Skips

Any skip must include why it was skipped, the command output, and the risk owner.
Do not set `ALLOW_LOCAL_QA_SKIP=1` unless the skipped result is recorded here.

| Gate | Skip reason | Evidence | Owner / follow-up |
| --- | --- | --- | --- |
| Installed app real paste | `/Applications/Foil.app` entered the mock async path, but this desktop session did not expose the TextEdit target AX window to the app process. | `make test-paste-real` exited `2`; `ALLOW_LOCAL_QA_SKIP=1 make test-paste-real` exited `0`. Diagnostics confirmed automation mock request, async path, production `insertAsync`, no UI-test paste queue, automation smoke enabled, and floating status stayed off by default. | Refresh Accessibility permission for `/Applications/Foil.app` and rerun `make test-paste-real` before public release if this skip is not accepted. |
| VS Code cross-app paste | Visual Studio Code.app is not installed on this runner. | `make test-cross-app` reported `VS Code: Visual Studio Code.app not installed`. | Install VS Code and rerun cross-app paste if VS Code coverage is required for the RC. |
| Notes cross-app paste | Test intentionally avoids mutating persistent Notes data from automation. | `make test-cross-app` reported `Notes: Skipped to avoid mutating persistent Notes data from automation`. | Use a disposable Notes account/container or manual Notes smoke test before public release if Notes is a required target. |
| Live cleanup quality | No `GROQ_API_KEY` was present in the shell. | `make test-cleanup-quality` failed fast because no provider key was available. | Rerun `GROQ_API_KEY=... make test-cleanup-quality` with a valid key before sign-off. |

## Release Artifact Verification

| Artifact | Command | Result | Notes |
| --- | --- | --- | --- |
| DMG signature | `spctl -a -vv -t open --context context:primary-signature Foil-1.8.2-macos.dmg` | PASS | Downloaded from `v1.8.2`; Gatekeeper accepted the DMG as `source=Notarized Developer ID`, origin `Developer ID Application: Mean Weasel LLC (B3A6AN2HA4)`. |
| DMG notarization staple | `xcrun stapler validate Foil-1.8.2-macos.dmg` | PASS | Stapler validation passed for the DMG. Mounted app does not have its own stapled ticket, but `spctl -a -vv /Volumes/Foil/Foil.app` accepted it as Notarized Developer ID and `codesign --verify --deep --strict --verbose=2` passed. |
| DMG checksum | `shasum -a 256 Foil-1.8.2-macos.dmg` | PASS | `b86a6d46d7aa987d6f5724c8aac574e9726c9bbbf1e0114389e49d1b869f2b8b`, matching the GitHub release asset digest. |
| Homebrew cask install | `brew install --cask foil` | BLOCKED | `gh repo view mean-weasel/homebrew-foil` must resolve and the tap cask must match the uploaded DMG checksum before Homebrew is treated as public-ready. README currently treats Homebrew as planned/unverified. |

## Manual Smoke

| Scenario | Result | Notes |
| --- | --- | --- |
| Fresh install from DMG | PASS | Copied `Foil.app` from mounted `Foil-1.8.2-macos.dmg` to `/Applications`, launched successfully, Gatekeeper accepted `/Applications/Foil.app`, bundle version/build `1.8.2` / `32`. |
| Accessibility permission prompt and setup check | PASS | Manual retry on the installed `/Applications/Foil.app` v1.12.0 build succeeded after eliminating a stale Debug/DerivedData process. Diagnostics show `AccessibilityTrust: launch initial=true`, `HotkeyMonitor: start succeeded`, and `SetupHealth: accessibilityTrusted=true`. |
| Microphone permission prompt and setup check | PASS | Manual permission flow succeeded. Diagnostics show `MicrophonePermission: authorizationStatus=0`, followed by `MicrophonePermission: requestAccess granted=true`, then successful audio capture and WAV write. |
| API key save and Keychain readiness | PASS | Manual Groq key Save/Test succeeded. Diagnostics show `validateApiKey: checking provider=groq requiredModels=2` and `validateApiKey: response status=200`. |
| One real transcription | PARTIAL | Installed app captured microphone audio and wrote a WAV after microphone consent. Full end-to-end transcription paste was not separately recorded in this manual step. |
| Paste fallback behavior in a blocked target app | PARTIAL | XCTest covers clipboard fallback restoration for terminated target. Manual blocked-target app smoke not run. |
| Clear History removes retained retry audio | PASS (TARGETED) | `xcodebuild test -scheme Foil -configuration Debug -destination 'platform=macOS' -parallel-testing-enabled NO -maximum-concurrent-test-device-destinations 1 -enableCodeCoverage NO -only-testing:FoilTests/TranscriptionHistoryTests` passed in `Test-Foil-2026.05.21_12-44-28--0700.xcresult`. The suite verifies failed audio is moved into `retry-audio`, survives reload for retry, and is removed by delete, clear history, retry resolution, retained-audio clear, and retention trimming. |

## Open Beta Readiness Continuation

| Gate | Command | Result | Artifact / notes |
| --- | --- | --- | --- |
| Unit XCTest suite after open-beta readiness changes | `make test` | PASS | xcresult: `Test-Foil-2026.05.21_11-57-23--0700.xcresult`; `** TEST SUCCEEDED **`. |
| Provider setup UI QA | `make test-provider-qa` | PASS | xcresult: `Test-Foil-2026.05.21_11-42-37--0700.xcresult`; all seven provider QA tests passed, including Groq default, Local whisper.cpp selection/setup/persistence, invalid custom URL, and custom provider persistence. |
| Full UI XCTest suite after open-beta readiness changes | `make test-ui` | BLOCKED | Bounded local run exceeded 600 seconds and left `Test-Foil-2026.05.21_11-46-41--0700.xcresult` incomplete/corrupt with no readable `Info.plist`. No lingering `xcodebuild`, `FoilUITests`, or `Foil.app` processes remained afterward. Rerun on a healthy idle UI automation session or complete equivalent manual smoke before beta sign-off. |
| Focused open-beta UI smoke | `xcodebuild test ... -only-testing:FoilUITests/FoilUITests/testSetupCheckCanBeRunInline ... testSettingsCanOpenSpecificTabs` | PASS | xcresult: `Test-Foil-2026.05.21_12-07-37--0700.xcresult`; 14 selected UI tests passed covering setup recovery, microphone setup states, onboarding completion, local-provider onboarding without API key, history search/filter/failure retry visibility, floating status, and settings tab routing. |
| Focused transcription cancel and hotkey accessibility UI smoke | `xcodebuild test ... -only-testing:FoilUITests/FoilUITests/testTranscribingStateShowsCancelTranscriptionAction -only-testing:FoilUITests/FoilUITests/testCustomHotkeyRecorderIsAccessibleButton` | PASS | xcresult: `Test-Foil-2026.05.21_12-18-04--0700.xcresult`; verified the transcribing state exposes an enabled `Cancel transcription` control that returns the session to Ready, and verified the custom hotkey recorder is exposed as an enabled accessibility button with the custom shortcut label. |
| Unit XCTest suite after focused UI harness additions | `make test` | PASS | xcresult: `Test-Foil-2026.05.21_12-18-44--0700.xcresult`; `** TEST SUCCEEDED **`. |
| Focused secondary UX UI smoke | `xcodebuild test ... -only-testing:FoilUITests/FoilUITests/testHistoryWindowOpensAndSearchesSeededRecords -only-testing:FoilUITests/FoilUITests/testFloatingWarningShowsExpandedClipboardContext -only-testing:FoilUITests/FoilUITests/testHelpButtonTargetsCanonicalTroubleshootingURL -only-testing:FoilUITests/FoilUITests/testCustomHotkeyRecorderIsAccessibleButton` | PASS | xcresult: `Test-Foil-2026.05.21_12-35-58--0700.xcresult`; verified history search and no-match empty state, floating warning title/detail/clipboard context, canonical troubleshooting Help URL capture, and custom hotkey recorder accessibility. |
| Unit XCTest suite after secondary UX UI harness additions | `make test` | PASS | xcresult: `Test-Foil-2026.05.21_12-37-08--0700.xcresult`; `** TEST SUCCEEDED **`. |
| Focused retained retry-audio cleanup suite | `xcodebuild test -scheme Foil -configuration Debug -destination 'platform=macOS' -parallel-testing-enabled NO -maximum-concurrent-test-device-destinations 1 -enableCodeCoverage NO -only-testing:FoilTests/TranscriptionHistoryTests` | PASS | xcresult: `Test-Foil-2026.05.21_12-44-28--0700.xcresult`; 43 history tests passed, including retained `retry-audio` persistence, delete, clear history, clear retained audio, retry resolution, and retention trimming cleanup. |
| Unit XCTest suite after retained-audio assertion tightening | `make test` | PASS | xcresult: `Test-Foil-2026.05.21_12-44-46--0700.xcresult`; `** TEST SUCCEEDED **`. |
| Focused Keychain storage suite | `xcodebuild test -scheme Foil -configuration Debug -destination 'platform=macOS' -parallel-testing-enabled NO -maximum-concurrent-test-device-destinations 1 -enableCodeCoverage NO -only-testing:FoilTests/KeychainHelperTests` | PASS | xcresult: `Test-Foil-2026.05.21_12-46-38--0700.xcresult`; 12 Keychain tests passed covering save/read, overwrite, delete, whitespace trimming, provider-scoped Groq/custom keys, and legacy plaintext migration/removal. Live installed-app Save/Test still requires a real or disposable Groq API key. |
| Live cleanup quality after open-beta readiness changes | `GROQ_API_KEY="$(security find-generic-password -a groq-api-key -w)" make test-cleanup-quality` | PASS | Used the local Keychain Groq key without printing it. The cleanup profile returned non-empty text and preserved the required core facts: tomorrow, demo, async paste, Chrome, Terminal, TextEdit, video, and Foil. |
| Homebrew tap availability | `gh repo view mean-weasel/homebrew-foil --json nameWithOwner,url,visibility` | PASS | Public tap exists at `https://github.com/mean-weasel/homebrew-foil`. |
| Homebrew cask metadata | `gh api repos/mean-weasel/homebrew-foil/contents/Casks/foil.rb` | PASS | Cask version is `1.12.0`; URL points to `https://github.com/mean-weasel/foil/releases/download/v1.12.0/Foil-1.12.0-macos.dmg`; `sha256` is `2f630fee780760bc3f0aef8a5d2aecbfbcd88d357cf5c7317d76b2a74991bd18`, matching the GitHub release asset digest. |
| Clean Homebrew cask install and launch smoke | `brew tap mean-weasel/foil https://github.com/mean-weasel/homebrew-foil`; `brew install --cask --appdir=/tmp/foil-brew-apps-74647 foil`; launch `Foil.app` with `--ui-testing --reset-defaults --seed-history`; `brew uninstall --cask foil`; `brew untap mean-weasel/foil` | PASS | Installed cask `foil` version `1.12.0` build `42` into a temporary app dir, launched successfully, `spctl` accepted the app as Notarized Developer ID, and `codesign --verify --deep --strict --verbose=2` passed. Cleanup removed the cask/temp app and restored the tap state. Existing `/Applications/Foil.app` remained version `1.8.2` build `32`. |
| `/Applications` v1.12.0 cask install and launch | `brew tap mean-weasel/foil https://github.com/mean-weasel/homebrew-foil`; move existing `/Applications/Foil.app` aside; `brew install --cask foil`; `open -a /Applications/Foil.app`; PlistBuddy version/build; `spctl --assess --type execute --verbose=4`; `codesign --verify --deep --strict --verbose=2` | PASS | Installed `/Applications/Foil.app` is version `1.12.0` build `42`, launched successfully, `spctl` accepted it as `source=Notarized Developer ID`, and deep strict codesign verification passed. The previous `1.8.2` build `32` app was moved to `/tmp/foil-app-backups/`. |
| Local permissions QA precheck | `make prepare-local-permissions-qa-check` | PASS WITH WARNINGS | Non-mutating check passed for the installed `/Applications/Foil.app` v1.12.0 bundle. It verified bundle id `com.neonwatty.Foil`, executable, microphone usage description, Developer ID authority `Mean Weasel LLC (B3A6AN2HA4)`, and codesign team id `B3A6AN2HA4`. The only warning was that Foil was running; macOS still requires manual Accessibility/Input Monitoring/Microphone consent and Test Setup. |
| Guided installed-app permissions QA helper | `make guide-installed-permissions-qa` | PASS WITH WARNINGS | Verified `/Applications/Foil.app` version `1.12.0` build `42`, Developer ID signing, microphone usage description, launched Foil, opened Accessibility, Input Monitoring, and Microphone privacy panes, and printed the release-smoke checklist. The only warning was that Foil was already running; consent toggles and API-key Save/Test remain manual. |
| Guided helper wrong-process guard | `make test-local-permissions-qa-script`; `make guide-installed-permissions-qa`; process inspection with `ps` | PASS | Helper now distinguishes a running `/Applications/Foil.app` release process from a Debug/DerivedData process and fails guide mode when the wrong Foil binary is active. Shell tests passed. Live guide confirmed the active process is `/Applications/Foil.app/Contents/MacOS/Foil`. |
| Accessibility retry diagnostics | `/usr/bin/log stream --debug ... com.apple.TCC ...`; `tail ~/Library/Application\\ Support/Foil/Diagnostics/foil.log` | PASS / MIC REMAINS | TCC attribution capture emitted no decision events during the setup check, but Foil diagnostics show the release app reached `AccessibilityTrust: launch initial=true`, `HotkeyMonitor: start succeeded`, and later `SetupHealth: accessibilityTrusted=true`. The same setup check still reports `microphone=notDetermined`, so remaining permission work is Microphone rather than Accessibility. |
| Manual fresh-install permission/API smoke | Installed `/Applications/Foil.app` v1.12.0; manual Accessibility and Microphone consent; Groq API-key Save/Test; Test Setup / recording path | PASS | User reported the flow worked. Diagnostics from the installed release process show Accessibility trusted, hotkey monitor started, Groq API validation returned HTTP 200, microphone access request was granted, and audio capture wrote `foil-6A652BA7-ED8A-4427-BC0B-664CDE368CEC.wav`. |
| Latest release assets | `gh release view --repo mean-weasel/foil --json tagName,url,assets,publishedAt,isDraft,isPrerelease,targetCommitish` | PARTIAL | Latest release `v1.12.0` is published with `Foil-1.12.0-macos.dmg` and `appcast.xml`. No separate `Foil-1.12.0-macos.dmg.sha256` asset is present; README now allows verification against the GitHub release asset digest or a `.sha256` file when published. |
| Sparkle appcast asset | `gh release download v1.12.0 --repo mean-weasel/foil --pattern appcast.xml` | PASS | `appcast.xml` points at `https://github.com/mean-weasel/foil/releases/download/v1.12.0/Foil-1.12.0-macos.dmg`, version `1.12.0`, build `42`, minimum macOS `14.0`. |
| v1.12.0 DMG checksum/signature/notarization | `gh release download v1.12.0 --repo mean-weasel/foil --pattern Foil-1.12.0-macos.dmg`; `shasum -a 256`; `spctl -a -vv -t open --context context:primary-signature`; `xcrun stapler validate` | PASS | SHA-256 is `2f630fee780760bc3f0aef8a5d2aecbfbcd88d357cf5c7317d76b2a74991bd18`, matching the GitHub release asset digest and Homebrew cask. Gatekeeper accepted the DMG as `source=Notarized Developer ID`, origin `Developer ID Application: Mean Weasel LLC (B3A6AN2HA4)`. Stapler validation passed. |
| v1.12.0 mounted app signature | `hdiutil attach -readonly -nobrowse`; `spctl -a -vv`; `codesign --verify --deep --strict --verbose=2`; `hdiutil detach` | PASS | Mounted `Foil.app` was accepted as Notarized Developer ID and `codesign` validated the app, Sparkle framework, updater app, and XPC services. The DMG was detached afterward. |

## v1.12.1 Post-Release Validation

| Gate | Command | Result | Artifact / notes |
| --- | --- | --- | --- |
| GitHub release assets | `gh release view v1.12.1 --repo mean-weasel/foil --json tagName,url,body,assets,publishedAt,isDraft,isPrerelease,targetCommitish` | PASS | Release `v1.12.1` is published, not draft, not prerelease, includes `Foil-1.12.1-macos.dmg`, `Foil-1.12.1-macos.dmg.sha256`, and `appcast.xml`, and has a Foil-focused release body. Rechecked 2026-05-27 after replacing stale legacy app asset names with byte-equivalent Foil-named assets. |
| Homebrew cask metadata | `gh api repos/mean-weasel/homebrew-foil/contents/Casks/foil.rb` | PASS | Cask version is `1.12.1`; URL points to `https://github.com/mean-weasel/foil/releases/download/v1.12.1/Foil-1.12.1-macos.dmg`; `sha256` is `4e3551cc66bda43191e2a73db61273f83d2fa50b2f26ddd85851f539f2298f9e`, matching the GitHub release asset digest and downloaded DMG. |
| `/Applications` cask reinstall | `brew update && brew reinstall mean-weasel/foil/foil` | PASS | Homebrew updated the `mean-weasel/foil` tap, removed the previous `1.12.0` app, and installed `/Applications/Foil.app` from cask `1.12.1`. |
| Installed app identity and signing | `PlistBuddy`; `codesign --verify --deep --strict --verbose=2`; `spctl -a -vv -t execute /Applications/Foil.app` | PASS | Installed app is version `1.12.1` build `33`. Deep strict codesign verification passed for the app, Sparkle framework, updater app, and XPC services. Gatekeeper accepted the installed app as `source=Notarized Developer ID`, origin `Developer ID Application: Mean Weasel LLC (B3A6AN2HA4)`. |
| Installed app staple check | `xcrun stapler validate /Applications/Foil.app` | INFO | The copied app bundle does not have its own stapled ticket. This matches the shipped artifact model verified below: the distributed DMG is stapled and Gatekeeper accepts the installed app as Notarized Developer ID. |
| DMG checksum/signature/notarization | `gh release download v1.12.1 --repo mean-weasel/foil --pattern 'Foil-1.12.1-macos.dmg' --pattern 'Foil-1.12.1-macos.dmg.sha256'`; `shasum -a 256`; `spctl -a -vv -t open --context context:primary-signature`; `xcrun stapler validate` | PASS | SHA-256 is `4e3551cc66bda43191e2a73db61273f83d2fa50b2f26ddd85851f539f2298f9e`, matching the `.sha256` asset, GitHub release digest, and Homebrew cask. Gatekeeper accepted the DMG as `source=Notarized Developer ID`; stapler validation passed. |
| Sparkle appcast asset | `gh release download v1.12.1 --repo mean-weasel/foil --pattern appcast.xml`; `xmllint --xpath ...` | PASS | `appcast.xml` points at `https://github.com/mean-weasel/foil/releases/download/v1.12.1/Foil-1.12.1-macos.dmg`, version `1.12.1`, build `33`, minimum macOS `14.0`, enclosure length `2299681`. |
| Guided installed-app permissions QA helper | `make guide-installed-permissions-qa` | PASS | Helper now derives expected version/build from the Xcode project. It verified `/Applications/Foil.app` version `1.12.1` build `33`, bundle id, executable, microphone usage description, Developer ID signing, and team id; launched Foil; opened Accessibility, Input Monitoring, and Microphone privacy panes; and printed the manual release-smoke checklist with 0 warnings. |
| Guided helper shell tests | `make test-local-permissions-qa-script`; `make -n guide-installed-permissions-qa` | PASS | Shell tests passed. Dry run shows `EXPECTED_VERSION="1.12.1" EXPECTED_BUILD="33" scripts/prepare-local-permissions-qa.sh --guide-installed`, preventing stale expected release values in future QA runs. |
| Installed app launch/setup diagnostics | `tail ~/Library/Application\\ Support/Foil/Diagnostics/foil.log`; `ps` | PASS | `/Applications/Foil.app/Contents/MacOS/Foil` launched. Diagnostics show `applicationDidFinishLaunching`, updater/controllers/hotkey configured, `AccessibilityTrust: launch initial=true`, `HotkeyMonitor: start succeeded`, `SetupHealth: accessibilityTrusted=true`, `SetupHealth: microphone=authorized`, and API key state refresh completed. |
| Manual first-run consent/API smoke | User-driven in Foil after helper opens privacy panes | NOT RERUN | macOS consent toggles and Groq API-key Save/Test remain manual. Existing diagnostics confirm the installed `1.12.1` app launched with Accessibility trusted and Microphone authorized; perform a fresh-profile manual Save/Test and hold-to-record smoke if beta sign-off requires new consent prompts rather than retained local grants. |

## v1.12.2 Post-Release Validation

| Gate | Command | Result | Artifact / notes |
| --- | --- | --- | --- |
| GitHub release assets | `gh release view v1.12.2 --repo mean-weasel/foil --json tagName,url,body,assets,publishedAt,isDraft,isPrerelease,targetCommitish` | PASS | Release `v1.12.2` is published, not draft, not prerelease, and includes `Foil-1.12.2-macos.dmg`, `Foil-1.12.2-macos.dmg.sha256`, and `appcast.xml`. Release notes were updated after workflow completion to include the icon and DMG presentation polish. |
| DMG checksum/signature/notarization | `gh release download v1.12.2 --repo mean-weasel/foil --pattern 'Foil-1.12.2-macos.dmg' --pattern 'Foil-1.12.2-macos.dmg.sha256' --pattern appcast.xml`; `shasum -a 256`; `spctl -a -vv -t open --context context:primary-signature`; `xcrun stapler validate` | PASS | SHA-256 is `39180396a7d29bd43c03165167823f91f4b7358a3937198f155a7eaae30574ad`, matching the `.sha256` asset and GitHub release asset digest. Gatekeeper accepted the DMG as `source=Notarized Developer ID`; stapler validation passed. |
| DMG contents and mounted app signing | `hdiutil attach -readonly -nobrowse`; `find /Volumes/Foil`; `PlistBuddy`; `spctl -a -vv -t execute`; `codesign --verify --deep --strict --verbose=2`; `hdiutil detach` | PASS | Mounted DMG contains `.background/dmg-background.png`, `Foil.app`, and `Applications`. Mounted app is version `1.12.2` build `34`, uses `AppIcon`, passes Gatekeeper, and satisfies deep strict codesign verification. |
| Release workflow DMG background | `gh run view 26524501743 --repo mean-weasel/foil --log --job 78124381631` | PASS | Workflow log shows `create-dmg` installed and copied `.github/assets/dmg-background.png` while creating `Foil-1.12.2-macos.dmg`; notarization and appcast upload passed. Local `screencapture` of the Finder window was unavailable in this desktop session. |
| Sparkle appcast asset | `xmllint /tmp/foil-1.12.2-release/appcast.xml`; inspect `appcast.xml` | PASS | `appcast.xml` points at `https://github.com/mean-weasel/foil/releases/download/v1.12.2/Foil-1.12.2-macos.dmg`, version `34`, short version `1.12.2`, minimum macOS `14.0`, enclosure length `2292946`. |
| Homebrew tap creation and cask metadata | `gh repo create mean-weasel/homebrew-foil`; `gh api repos/mean-weasel/homebrew-foil/contents/Casks/foil.rb` | PASS | Public tap `mean-weasel/homebrew-foil` exists and `Casks/foil.rb` points at `v1.12.2` with SHA-256 `39180396a7d29bd43c03165167823f91f4b7358a3937198f155a7eaae30574ad`. |
| Clean Homebrew cask install and launch smoke | `brew tap mean-weasel/foil https://github.com/mean-weasel/homebrew-foil`; `brew install --cask --appdir=/tmp/foil-brew-apps-35355 mean-weasel/foil/foil`; `PlistBuddy`; `spctl`; `codesign`; `brew uninstall`; `brew untap` | PASS | Homebrew installed Foil `1.12.2` build `34` into a temporary app dir. Gatekeeper accepted the app as Notarized Developer ID and deep strict codesign verification passed. Cleanup removed the temp app and untapped the tap. |
| Fresh public Homebrew cask smoke recheck | `brew tap mean-weasel/foil https://github.com/mean-weasel/homebrew-foil`; `brew info --cask mean-weasel/foil/foil`; `brew install --cask --appdir=/tmp/foil-brew-apps-1779902384 mean-weasel/foil/foil`; `PlistBuddy`; `spctl -a -vv -t execute`; `codesign --verify --deep --strict --verbose=2`; `brew uninstall`; `brew untap`; `rm -rf /tmp/foil-brew-apps-1779902384` | PASS | Public cask reported `1.12.2`, installed into a temporary app dir as Foil `1.12.2` build `34`, Gatekeeper accepted it as `source=Notarized Developer ID` with origin `Developer ID Application: Mean Weasel LLC (B3A6AN2HA4)`, and deep strict codesign verification passed. Cleanup removed the cask, untapped the tap, and removed the temp app dir. |

## v1.13.8 Post-Release Validation

| Gate | Command | Result | Artifact / notes |
| --- | --- | --- | --- |
| Separate-machine local smoke | Issue #331 run-from-build smoke | PASS | Built source `18d5ccc` locally, verified Settings > What's New exposed current version/build and update control, and verified Groq/OpenAI cleanup connection tests show clear missing-key messages without raw `TranscriptionError`. Issue #331 was closed after release validation. |
| Release workflow | `gh workflow run deploy.yml -f version=1.13.8 -f build=43`; run `28266487080` | PASS | Workflow passed DMG build/notarization/upload, notarization validation, appcast generation/upload, and Homebrew cask update. |
| GitHub release assets | `gh release view v1.13.8 --repo usefoil/foil --json tagName,url,assets,publishedAt,isDraft,isPrerelease,targetCommitish` | PASS | Release `v1.13.8` is published, not draft, not prerelease, and includes `Foil-1.13.8-macos.dmg`, `Foil-1.13.8-macos.dmg.sha256`, and `appcast.xml`. |
| DMG checksum/signature/notarization | `shasum -a 256`; normalized `.sha256` check; `spctl -a -vv -t open --context context:primary-signature`; `xcrun stapler validate` | PASS | SHA-256 is `88514efe98d14392644b2d5b1d74ddb6199fad0ab4faca3091ae033c755ad848`, matching the `.sha256` asset and GitHub release asset digest. Gatekeeper accepted the DMG as `source=Notarized Developer ID`; stapler validation passed. The `.sha256` file records the GitHub runner path, so local `shasum -c` was run with the filename normalized. |
| DMG contents and mounted app signing | `hdiutil attach -readonly -nobrowse`; `find /Volumes/Foil`; `PlistBuddy`; `spctl -a -vv -t execute`; `codesign --verify --deep --strict --verbose=2`; `hdiutil detach` | PASS | Mounted DMG contains `.background/dmg-background.png`, `Foil.app`, and `Applications`. Mounted app is bundle id `com.neonwatty.Foil`, version `1.13.8`, build `43`, includes `SUPublicEDKey`, passes Gatekeeper as Notarized Developer ID, and satisfies deep strict codesign verification. |
| Sparkle appcast asset | `xmllint --noout appcast.xml`; inspect appcast fields | PASS | `appcast.xml` points at `https://github.com/usefoil/foil/releases/download/v1.13.8/Foil-1.13.8-macos.dmg`, includes `sparkle:version` `43`, `sparkle:shortVersionString` `1.13.8`, `sparkle:edSignature`, and `sparkle:length` `2726608`. |
| Homebrew cask metadata | `gh api repos/mean-weasel/homebrew-foil/contents/Casks/foil.rb`; `brew info --cask mean-weasel/foil/foil` | PASS | Public cask reports version `1.13.8` with SHA-256 `88514efe98d14392644b2d5b1d74ddb6199fad0ab4faca3091ae033c755ad848`, matching the release DMG. |
| Temp-dir Homebrew cask identity smoke | `brew install --cask --appdir=/tmp/foil-brew-apps-1.13.8-78326 mean-weasel/foil/foil`; `PlistBuddy`; `spctl`; `codesign`; `brew uninstall`; `rm -rf` | PASS | Installed cask into a temporary app dir as Foil `1.13.8` build `43`; `SUPublicEDKey` was present; Gatekeeper accepted the app as Notarized Developer ID; deep strict codesign verification passed; cleanup removed the cask/temp app. Homebrew emitted unrelated local tap-trust warnings for other taps. |
| Temp-dir Homebrew cask launch smoke | `brew install --cask --appdir=/tmp/foil-brew-apps-1.13.8-launch-79748 mean-weasel/foil/foil`; `open -n .../Foil.app --args --ui-testing --reset-defaults --seed-history`; process-path check; `brew uninstall`; `rm -rf` | PASS | The temp-installed app launched successfully from `/private/tmp/foil-brew-apps-1.13.8-launch-79748/Foil.app/Contents/MacOS/Foil`; cleanup removed the cask/temp app. |
| `/Applications` production install | Move existing `/Applications/Foil.app` to `/tmp/foil-app-backups/20260626-153116-Foil-1.13.5-build40.app`; `brew install --cask mean-weasel/foil/foil`; `make guide-production-permissions-qa` | PASS | Backed up the previous local-signed `1.13.5` build `40` app, installed public cask `1.13.8` build `43` into `/Applications`, verified Developer ID signing/team id `B3A6AN2HA4`, Gatekeeper `Notarized Developer ID`, deep strict codesign, and active process path `/Applications/Foil.app/Contents/MacOS/Foil`. Helper opened Accessibility, Input Monitoring, and Microphone privacy panes and passed with 0 warnings. |
| `/Applications` quit/relaunch persistence | `pkill -x Foil`; `open /Applications/Foil.app`; `pgrep -x Foil`; `ps`; `PlistBuddy` | PASS | App relaunched as pid `88095` from `/Applications/Foil.app/Contents/MacOS/Foil`; installed app still reported version `1.13.8` build `43`. Did not reset TCC on this daily account. |
| Fresh Foil-scoped TCC permission pass | `tccutil reset Accessibility com.neonwatty.Foil`; `tccutil reset ListenEvent com.neonwatty.Foil`; `tccutil reset Microphone com.neonwatty.Foil`; user-approved Accessibility toggle for `Foil`; `tccutil reset Microphone com.neonwatty.Foil`; `killall tccd`; relaunch `/Applications/Foil.app --show-onboarding`; in-app `Check Microphone Access`; System Settings Microphone inspection | PARTIAL / MICROPHONE BLOCKED | Fresh reset proved the notarized `/Applications` app still launches as Foil `1.13.8` build `43` and Accessibility can be granted: diagnostics changed from `SetupHealth: accessibilityTrusted=false` to `SetupHealth: accessibilityTrusted=true`, and `HotkeyMonitor: permission refresh start succeeded`. Microphone remained blocked: diagnostics logged `MicrophonePermission: authorizationStatus=0`, `requestAccess timed out after 15.0s`, `requestAccess timedOut=true`, and `SetupHealth: microphone=notDetermined` on two attempts after the documented TCC recovery. System Settings > Privacy & Security > Microphone did not show a `Foil` row after either attempt. Follow-up: issue #334. |
| Fresh TCC continuation after manual Microphone grant | User granted Microphone in System Settings; Foil onboarding Microphone step inspection; `Get Started`; `pkill -x Foil`; `open -n /Applications/Foil.app`; `pgrep`; `ps`; `PlistBuddy`; diagnostics inspection | PASS | After the user granted Microphone, Foil diagnostics reported `SetupHealth: microphone=authorized`, `SetupHealth: inputDevices count=2 selectedUID=systemDefault microphoneState=ready`, and the onboarding Microphone step showed ready with `Get Started` enabled. After completing onboarding and relaunching, `/Applications/Foil.app` still reported version `1.13.8` build `43`; diagnostics showed `AccessibilityTrust: launch initial=true`, `HotkeyMonitor: start succeeded`, `SetupHealth: accessibilityTrusted=true`, and `SetupHealth: microphone=authorized`. The earlier prompt-timeout failure remains tracked in issue #334. |
| Issue #334 microphone timeout recovery patch | `xcodebuild test ... -only-testing:FoilTests/AppStateTests/testMicrophonePromptTimeoutShowsManualPrivacyRecovery -only-testing:FoilTests/SingleInstanceGuardTests/testAppDelegateMicrophoneCheckShowsRecoveryWhenRequestTimesOut`; `xcodebuild test ... -only-testing:FoilUITests/FoilUITests/testMicrophoneTimeoutShowsManualPrivacyRecovery`; `git diff --check` | PASS | Timeout recovery now starts setup-health polling and keeps a specific `Open Microphone privacy and allow Foil` state instead of collapsing to generic relaunch/reset guidance. Unit tests passed with xcresult `Test-Foil-2026.06.27_11-20-16--0700.xcresult`; UI passed with xcresult `Test-Foil-2026.06.27_11-20-30--0700.xcresult`, proving the visible control-center text and `Open Settings` action. An earlier UI attempt failed before app assertions because `FoilUITests-Runner` timed out enabling automation mode, then passed on rerun. |

## v1.13.9 Post-Release Validation

| Gate | Command | Result | Artifact / notes |
| --- | --- | --- | --- |
| Release workflow | `gh workflow run deploy.yml -f version=1.13.9 -f build=44`; successful run `28327378895` | PASS | Workflow passed tag verification, DMG build/notarization/upload, notarization validation, appcast generation/upload, Homebrew cask update, and keychain cleanup. Earlier run `28326080906` failed during Release archive compile because a Release build referenced the DEBUG-only `simulateSelectedHotkeyCycleForUITesting`; PR #341 fixed that failure, the empty assetless release/tag was removed, and `v1.13.9` was recreated at `0dcc3287327ae91767bcfe3f806de4ce8fc852d5`. |
| GitHub release assets | `gh release view v1.13.9 --json tagName,targetCommitish,isDraft,isPrerelease,publishedAt,assets,url` | PASS | Release `v1.13.9` is published, not draft, not prerelease, and includes `Foil-1.13.9-macos.dmg`, `Foil-1.13.9-macos.dmg.sha256`, and `appcast.xml`. |
| Required release commit | `REQUIRED_COMMIT=0dcc3287327ae91767bcfe3f806de4ce8fc852d5 make check-production-permissions-cask` | PASS | Latest release `v1.13.9` contains the release-fix commit `0dcc3287327ae91767bcfe3f806de4ce8fc852d5`. |
| DMG checksum/signature/notarization | `shasum -a 256`; `.sha256` inspection; `spctl --assess --type open --context context:primary-signature -v`; `xcrun stapler validate` | PASS | SHA-256 is `aff1b812f184604478b1463d7e40a358bb889fbef9d20e6ccf91e45311bca83e`, matching the `.sha256` asset, GitHub release asset digest, and Homebrew cask. Gatekeeper accepted the DMG as `source=Notarized Developer ID`; stapler validation passed. |
| DMG contents and mounted app signing | `hdiutil attach -readonly -nobrowse`; `find`; `PlistBuddy`; `spctl -a -vv -t execute`; `codesign --verify --deep --strict --verbose=2`; `hdiutil detach` | PASS | Mounted DMG contains `.background/dmg-background.png`, `.DS_Store`, `Foil.app`, and `Applications`. Mounted app is bundle id `com.neonwatty.Foil`, version `1.13.9`, build `44`, includes `SUPublicEDKey`, passes Gatekeeper as Notarized Developer ID, and satisfies deep strict codesign verification. |
| Sparkle appcast asset | `xmllint --noout /tmp/foil-release-1.13.9/appcast.xml`; inspect appcast fields | PASS | `appcast.xml` points at `https://github.com/usefoil/foil/releases/download/v1.13.9/Foil-1.13.9-macos.dmg`, includes `sparkle:edSignature`, and includes `sparkle:length="2727146"`. |
| Homebrew cask metadata | `gh api repos/mean-weasel/homebrew-foil/contents/Casks/foil.rb`; `brew update`; `brew info --cask mean-weasel/foil/foil` | PASS | Public cask reports version `1.13.9` with SHA-256 `aff1b812f184604478b1463d7e40a358bb889fbef9d20e6ccf91e45311bca83e`, matching the release DMG. |
| Temp-dir Homebrew cask identity smoke | `REQUIRED_COMMIT=0dcc3287327ae91767bcfe3f806de4ce8fc852d5 make check-production-permissions-cask` | PASS | Installed public cask into `/tmp/foil-release-apps` as Foil `1.13.9` build `44`; bundle id, `SUPublicEDKey`, Developer ID authority/team id, Gatekeeper `Notarized Developer ID`, and deep strict codesign all passed. |
| `/Applications` production install | `brew upgrade --cask mean-weasel/foil/foil`; `make guide-production-permissions-qa` | PASS | Upgraded `/Applications/Foil.app` from `1.13.8` build `43` to `1.13.9` build `44`, verified Developer ID signing/team id `B3A6AN2HA4`, Gatekeeper `Notarized Developer ID`, deep strict codesign, and active process path `/Applications/Foil.app/Contents/MacOS/Foil`. Helper passed with 0 warnings. |
| Fresh-machine TCC matrix | `docs/fresh-machine-homebrew-onboarding-smoke.md` | PENDING OPERATOR | Not run on this daily-driver account during v1.13.9 release. Do not run destructive TCC resets here without explicit operator approval; use a fresh macOS account, VM, spare Mac, or disposable user for the full onboarding matrix. |

## v1.13.10 TCC Matrix Continuation

| Gate | Command / steps | Result | Artifact / notes |
| --- | --- | --- | --- |
| `/Applications` production identity | `PlistBuddy`; `pgrep`; `ps`; `spctl -a -vv -t execute /Applications/Foil.app`; `codesign --verify --deep --strict --verbose=2 /Applications/Foil.app`; `codesign -dvvv --entitlements :- /Applications/Foil.app`; `make guide-production-permissions-qa` | PASS | Installed app is `/Applications/Foil.app`, bundle id `com.neonwatty.Foil`, version `1.13.10`, build `45`, running from `/Applications/Foil.app/Contents/MacOS/Foil`. Gatekeeper reports `source=Notarized Developer ID`, origin `Developer ID Application: Mean Weasel LLC (B3A6AN2HA4)`, deep strict codesign passed, signing identifier matches the bundle id, team id is `B3A6AN2HA4`, hardened runtime is enabled, and entitlements include Apple Events automation plus audio input. Production guide passed with 0 warnings and opened Accessibility, Input Monitoring, and Microphone privacy panes. |
| Accessibility already granted | Relaunch `/Applications/Foil.app` in inspection mode with `--ui-testing --show-onboarding`, no `--reset-defaults`, no permission seed flags, and `FOIL_UITEST_COMMAND_PATH=/tmp/foil-tcc-matrix-command.json`; send onboarding command `goToAccessibility`; inspect with System Events. | PASS | Live state snapshot `/tmp/foil-tcc-matrix-state.json` reported `accessibilityText:"Ready"`, `microphoneText:"Ready"`, `apiKeyText:"Ready"`, `sessionTitle:"Ready"`, and `canStartRecording:true`. Accessibility step AX tree showed `Accessibility Permission`, explanatory copy, and `Ready`; `Enable Accessibility` was not present. |
| Microphone already granted | Same signed app inspection mode; send onboarding command `goToMicrophone`; inspect with System Events. | PASS | Microphone step AX tree showed `Microphone Access`, explanatory copy, and `Ready`. State snapshot still reported `microphoneText:"Ready"` and `canStartRecording:true`. |
| Quit/relaunch persistence | `pkill -x Foil`; `open -n -F /Applications/Foil.app`; `pgrep`; `ps`; `PlistBuddy`; diagnostics inspection. | PASS | Normal production app relaunched as pid `13547` from `/Applications/Foil.app/Contents/MacOS/Foil`, still version `1.13.10` build `45`. Diagnostics at `2026-07-04T02:23:27Z` showed `AccessibilityTrust: launch initial=true`, `HotkeyMonitor: start succeeded`, `SetupHealth: accessibilityTrusted=true`, `SetupHealth: microphone=authorized`, `SetupHealth: inputDevices count=2 selectedUID=systemDefault microphoneState=ready`, Keychain read success, and `SetupHealth: API key state refreshed`. |
| Issue #334 microphone reset probe | `pkill -x Foil`; `tccutil reset Microphone com.neonwatty.Foil`; `killall tccd`; relaunch `/Applications/Foil.app`; open Foil status surface; click the Microphone row action; wait through the 15s request timeout; inspect diagnostics and System Settings > Privacy & Security > Microphone. | BLOCKED / REPRODUCED | `tccutil` reported `Successfully reset Microphone approval status for com.neonwatty.Foil`. The normal production app relaunched as pid `19814` from `/Applications/Foil.app/Contents/MacOS/Foil`; diagnostics at `2026-07-04T02:25:35Z` showed `SetupHealth: microphone=notDetermined`. Clicking the Microphone action produced `MicrophonePermission: authorizationStatus=0`, then `MicrophonePermission: requestAccess timed out after 15.0s`, `requestAccess timedOut=true`, `SetupHealth: microphone=notDetermined`, and `MicrophonePermission: checked ready=false`. The visible Foil surface stayed in `Setup needed`; Accessibility was `Ready`, Microphone was `Not checked`, and the message was `Check microphone access before recording`. System Settings Microphone text inspection showed `Foil Dev` but no production `Foil` row after the request. This reproduces issue #334 on `1.13.10` build `45`; the production app remains in `microphone=notDetermined` on this account after the test. |
| Issue #334 fix candidate | Code patch switches `SystemSetupPermissionProvider` from `AVCaptureDevice.requestAccess(for: .audio)` / `authorizationStatus(for: .audio)` to `AVAudioApplication.requestRecordPermission` / `AVAudioApplication.shared.recordPermission`, preserving the existing timeout recovery. `xcodebuild test -scheme Foil -configuration Debug -destination 'platform=macOS' -parallel-testing-enabled NO -maximum-concurrent-test-device-destinations 1 -enableCodeCoverage NO -only-testing:FoilTests/SingleInstanceGuardTests`; `xcodebuild test -scheme Foil -configuration Debug -destination 'platform=macOS' -parallel-testing-enabled NO -maximum-concurrent-test-device-destinations 1 -enableCodeCoverage NO -only-testing:FoilTests/AppStateTests`; `xcodebuild build -scheme Foil -configuration Release -destination 'platform=macOS'`; `git diff --check`. | LOCAL PASS / PRODUCTION PROOF PENDING | `SingleInstanceGuardTests` passed 28 tests, including `testSystemPermissionProviderMapsAudioApplicationRecordPermission` and the timeout-recovery test; xcresult `/Users/jeremywatt/Library/Developer/Xcode/DerivedData/Foil-ebuxbvgmspyykabsrfclwquwxbdi/Logs/Test/Test-Foil-2026.07.03_19-35-19--0700.xcresult`. `AppStateTests` passed 156 tests, including setup-health microphone authorization mapping; xcresult `/Users/jeremywatt/Library/Developer/Xcode/DerivedData/Foil-ebuxbvgmspyykabsrfclwquwxbdi/Logs/Test/Test-Foil-2026.07.03_19-36-12--0700.xcresult`. Release build succeeded against macOS SDK 26.2 with deployment target macOS 14.0, proving the new AVFAudio API compiles in the Release target. This does not close the TCC matrix until a Developer ID signed, notarized build containing this patch is installed at `/Applications/Foil.app` and the microphone reset probe is rerun. |
| Full fresh-machine TCC matrix | Fresh install, Accessibility grant-while-on-step, Microphone not-determined prompt grant, and permission-revoked-while-running rows from `docs/fresh-machine-homebrew-onboarding-smoke.md`. | NOT RUN | This daily-driver account already has Foil app data and retained permission history. Do not treat it as issue #154 fresh-user proof. Run these remaining rows on a disposable macOS user, VM, spare Mac, or freshly erased machine, or get explicit operator approval before broader Foil-scoped Accessibility/All TCC resets on this account. |

## Queued Paste Compatibility Smoke

| Gate | Command | Result | Artifact / notes |
| --- | --- | --- | --- |
| Runbook and matrix | `docs/queued-paste-compatibility-smoke.md` | ADDED | Defines TextEdit, browser text-entry, and unavailable-target rows. The matrix requires app/window identity evidence instead of treating queued paste as a screen-location feature. |
| Local prerequisite harness | `ALLOW_LOCAL_QA_SKIP=1 make test-queued-paste-compatibility` | PASS WITH EXPLICIT LOCAL SKIP | Artifacts: `/tmp/foil-queued-paste-compatibility-20260528-042455`. Installed-app TextEdit automation reached production `insertAsync` and avoided the UI-test paste bypass, but skipped because `/Applications/Foil.app` did not receive the target AX window in this desktop session. Cross-app target checks passed for TextEdit async paste, SkyLight background paste, Terminal, and Chrome textarea. |
| Real-target queued automation hook | `ALLOW_LOCAL_QA_SKIP=1 make test-queued-paste-compatibility` | PARTIAL | Artifacts: `/tmp/foil-queued-paste-compatibility-20260528-043456`. New automation hook enqueued and delivered queued items against real TextEdit and Chrome pids, and unavailable-target fallback passed with clipboard recovery. TextEdit and Chrome delivery did not land because `/Applications/Foil.app` is `accessibilityTrusted=false` in this desktop session and captured no AX window. |
| Real-target queued rerun | `ALLOW_LOCAL_QA_SKIP=1 make test-queued-paste-compatibility`; `make prepare-local-permissions-qa-check` | BLOCKED BY LOCAL SIGNING/TCC IDENTITY | Artifacts: `/tmp/foil-queued-paste-compatibility-20260528-044803`. Cross-app prerequisites still passed and unavailable-target fallback passed. TextEdit/Chrome queued delivery still did not land. The installed app precheck reports `Info.plist` bundle id `com.neonwatty.Foil`, but `codesign` identifier `Foil`, ad-hoc signing, and no team id; Foil diagnostics show `SetupHealth: accessibilityTrusted=false`. Reinstall with a trusted signing identity, refresh Accessibility/Input Monitoring for `/Applications/Foil.app`, then rerun. |
| Signed-app queued rerun | `make setup-local-signing LOCAL_SIGN_KEYCHAIN_PASSWORD=foil-local-codesign`; `security unlock-keychain`; `security set-key-partition-list`; `make install SIGN_IDENTITY="Foil Local Code Signing"`; `ALLOW_LOCAL_QA_SKIP=1 make test-queued-paste-compatibility` | BLOCKED BY ACCESSIBILITY CONSENT | Artifacts: `/tmp/foil-queued-paste-compatibility-20260528-050432`. Installed app identity now passes: `codesign` reports identifier `com.neonwatty.Foil` with authority `Foil Local Code Signing`, and the identity precheck passes. TextEdit/Chrome queued delivery still did not land because Foil diagnostics still show `SetupHealth: accessibilityTrusted=false`. Unavailable-target fallback still passes. Refresh Accessibility/Input Monitoring for the newly signed `/Applications/Foil.app`, then rerun. |
| Trusted-app product-fix queued rerun | `swiftc -parse tests/test_queued_paste_compatibility.swift`; `xcodebuild test ... -only-testing:FoilTests/BackgroundPasteTests`; `make prepare-local-permissions-qa-check`; `ALLOW_LOCAL_QA_SKIP=1 make test-queued-paste-compatibility` | PASS | Artifacts: `/tmp/foil-queued-paste-compatibility-20260528-081907`; xcresult: `Test-Foil-2026.05.28_08-18-35--0700.xcresult`. Diagnostics show `SetupHealth: accessibilityTrusted=true`. TextEdit, Chrome, and unavailable-target fallback all passed. Chrome direct AX selected-text insertion reported success but did not change the value, so Foil fell back to Tier 2 choreography and delivered via `original app command posted`. |
| Browser/fallback expansion queued rerun | `swiftc -parse tests/test_queued_paste_compatibility.swift`; `swiftc -parse tests/test_cross_app_async_paste.swift`; `xcodebuild test ... -only-testing:FoilTests/QueuedPasteTests`; `ALLOW_LOCAL_QA_SKIP=1 make test-queued-paste-compatibility` | PASS WITH OPTIONAL FIREFOX SKIP | Artifacts: `/tmp/foil-queued-paste-compatibility-20260528-091151`; xcresult: `Test-Foil-2026.05.28_09-06-58--0700.xcresult`. TextEdit, Chrome, Safari, and unavailable-target fallback passed. Firefox is installed, but exposed `Problem loading page` for the disposable local-file target, so the optional row skipped instead of pasting into an arbitrary Firefox page. Cross-app Chrome no longer closes the active tab. Fallback row recorded `Target unavailable; text copied to clipboard`. |
| Localhost browser/fallback queued rerun | `swiftc -parse tests/test_queued_paste_compatibility.swift`; `swiftc -parse tests/test_cross_app_async_paste.swift`; `xcodebuild test ... -only-testing:FoilTests/QueuedPasteTests`; `ALLOW_LOCAL_QA_SKIP=1 make test-queued-paste-compatibility` | PASS | Artifacts: `/tmp/foil-queued-paste-compatibility-20260528-095356`; xcresult: `Test-Foil-2026.05.28_09-51-55--0700.xcresult`. TextEdit, Chrome, Firefox, Safari, and unavailable-target fallback passed. Browser targets use localhost; Chrome/Firefox request private windows; Chrome/Firefox/Safari rows all record `noTabClose=true` and `noUserBrowserQuit=true`. Cross-app Chrome passed with localhost/private-mode evidence. Fallback row recorded `Target unavailable; text copied to clipboard`. |
| TextEdit queued-paste row | `tests/test_queued_paste_compatibility.swift` | PASS | Trusted-app rerun artifact `/tmp/foil-queued-paste-compatibility-20260528-081907`: `TextEdit pid=57807`, title `FoilQueuedTextEditTarget.txt`, queued delivery passed. |
| Browser queued-paste row | `tests/test_queued_paste_compatibility.swift` | PASS | Latest artifact `/tmp/foil-queued-paste-compatibility-20260528-095356`: Chrome passed against localhost in an Incognito window with `noTabClose=true` and `noUserBrowserQuit=true`; Firefox passed against localhost in Private Browsing with the same safety flags; Safari passed against a disposable localhost target with no tab close or browser quit path. |
| Unavailable-target fallback row | `tests/test_queued_paste_compatibility.swift` | PASS | Queued TextEdit target, quit TextEdit before delivery, observed clipboard fallback, verified the clipboard contained the queued transcript text, and recorded recovery message `Target unavailable; text copied to clipboard`. |

## Other Audio Recording Policy QA

| Gate | Command / steps | Result | Artifact / notes |
| --- | --- | --- | --- |
| Default-safe policy tests | Focused `xcodebuild test` rows for `AppStateTests`, `BrowserMediaControllerTests`, and `DiagnosticLogTests` | PASS | Current-state xcresults: `Test-Foil-2026.06.01_11-12-54--0700.xcresult` and `Test-Foil-2026.06.01_11-13-12--0700.xcresult`. Tests cover default `pauseBrowserMediaWhileRecording == false`, persisted opt-in behavior, `otherAudio: unaffected policy=none`, opt-in browser media diagnostics, and diagnostics/setup report policy strings. |
| Bluetooth input treatment tests | Focused `xcodebuild test` rows for `AudioRecorderTests` and `DiagnosticLogTests` | PASS | Current-state xcresults: `Test-Foil-2026.06.01_11-12-54--0700.xcresult` and `Test-Foil-2026.06.01_11-13-12--0700.xcresult`. Tests cover public Core Audio transport mapping, Bluetooth/Bluetooth LE identification, user-readable transport names, available-device transport metadata, and diagnostic reporting. |
| Debug app build | `xcodebuild -project Foil.xcodeproj -scheme Foil -destination 'platform=macOS' build` | PASS | Built `/Users/jeremywatt/Library/Developer/Xcode/DerivedData/Foil-fxfrtzwwzpscfhcbntvmjqudewab/Build/Products/Debug/Foil.app` on 2026-06-01. Xcode emitted the existing CoreSimulator warning, but the macOS app build succeeded. |
| Local Bluetooth-risk precheck | `system_profiler SPAudioDataType` | PASS / RISK PRESENT | AirPods are default input over Bluetooth and default output over Bluetooth; Mac mini Speakers are default system output over Built-in. This is valid hardware state for checking the new Bluetooth input warning. |
| Dev app Recording settings visual check | Launch debug app with `--ui-testing --reset-defaults --seed-history --settings-tab-recording`, open Settings via UI, inspect Recording pane | PARTIAL | Verified the other-audio toggle is off by default and the help copy is bounded to Chrome/Chromium. Bluetooth warning could not be visually executed because the local audio state had changed: `system_profiler SPAudioDataType` reported only Mac mini Speakers and a Core Audio default-input query returned device `0`. Debug app process was quit afterward. |
| Dev app setup report runtime check | Launch debug app with `--ui-testing --reset-defaults --seed-history --settings-tab-recording`, click `Copy Setup Report`, inspect copied report lines | PASS | Runtime setup report included `Input Device UID: System Default`, `Input Device Transport: Unknown`, and `Other Audio While Recording: unaffected`. No recording was started and the debug app process was quit afterward. |
| Manual audio smoke matrix | `docs/goals/other-audio-recording-policy/notes/T007-manual-audio-smoke-matrix.md` | PENDING OPERATOR | Remaining proof requires human auditory confirmation for browser media with the setting off/on, non-browser audio with non-Bluetooth input when available, and Bluetooth headset mic warning/behavior. |

## Pre-Release Feature Hardening

| Gate | Command / steps | Result | Artifact / notes |
| --- | --- | --- | --- |
| Cleanup group running-app picker filter | `git diff --check`; `xcodebuild test -project Foil.xcodeproj -scheme Foil -only-testing:FoilTests/CleanupGroupTests -only-testing:FoilUITests/FoilUITests/testCleanupGroupAppPickerShowsRecentlyUsedAppsFromUsageEvents -only-testing:FoilUITests/FoilUITests/testCleanupGroupRunningAppsFilterExcludesFalsePositiveApps`; launch Debug app with `--ui-testing --reset-defaults --seed-usage-events --show-app-shell`; capture app-shell screenshot. | PASS | Unit tests prove Foil/self, Finder, Photos, System Settings, Activity Monitor, Preview, and App Store are filtered from running-app suggestions while Ghostty, Terminal, Codex, Google Chrome, Messages (iMessage), and Mail are not denylisted. UI tests prove seeded recent apps and `Choose .app...` remain available. Screenshot: `/tmp/foil-feature-screenshots/cleanup-filtered-running-apps.png`. Debug screenshot process was quit; installed `/Applications/Foil.app` remained running as pid `19814`. |

## Sign-Off

- Known P0/P1 issues: None from CI/local deterministic gates, `v1.12.2` release workflow, DMG verification, temp-dir Homebrew public cask smoke, mounted-app signing/Gatekeeper checks, or installed-app launch/setup diagnostics.
- Remaining P2/P3 issues accepted for this release: Installed-app paste AX-window skip on this runner; VS Code/Notes cross-app coverage skipped.
- Approver:
